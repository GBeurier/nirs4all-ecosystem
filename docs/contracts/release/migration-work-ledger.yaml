schema_version: n4a.migration-work-ledger/v1
program: nirs4all-native-backend-v1
authoritative_sources:
  audit: /home/delete/nirs4all/AUDIT_MIGRATION_BACKEND_RUST_V1.md
  roadmap: /home/delete/nirs4all/ROADMAP_BACKEND_NATIF_V1.md
  release_lock: docs/contracts/release/aggregation-lock.n4a.lock.json
  release_manifest: docs/contracts/release/aggregation-manifest.n4a.json
  public_surface_matrix: docs/contracts/release/public-v1-surface-matrix.n4a.json
release_train:
  shape: four_release_v1_with_explicit_rollback_profile
  reason: >-
    ADR-24 maps R2 as the native-default flip and R3/R4 as the two complete
    post-flip rollback-retention releases. Studio and the default native path
    are strict in R3/R4; the separately selected Python engine=legacy profile
    remains installed through R4, without implicit fallback or reinstall.
initialization:
  timestamp_utc: 2026-08-20T13:52:58Z
  workspace: /home/delete/nirs4all
  ecosystem_sha: a34126d43c68e5122ba35a5ac8e2b36e4da56b61
  release_lock_validation:
    status: passed_selected_workspace
    command: >-
      python3 scripts/n4a_release_lock.py checkout-members --manifest
      docs/contracts/release/aggregation-manifest.n4a.json --lock
      docs/contracts/release/aggregation-lock.n4a.lock.json --output
      <selected-release-members> && python3 scripts/n4a_release_lock.py
      --workspace-root <selected-release-members> validate --manifest
      docs/contracts/release/aggregation-manifest.n4a.json --lock
      docs/contracts/release/aggregation-lock.n4a.lock.json
    evidence: >-
      On 2026-08-31, checkout-members reconstructed the lock-pinned root and
      the release-lock, native-capability-ledger and public-surface-matrix
      validators all succeeded against it. The developer sibling root remains
      intentionally unsuitable as release evidence whenever a selected member
      is on an active non-main branch; CI must use the reconstructed root.
  surface_matrix_validation:
    status: passed
    command: >-
      python3 scripts/n4a_release_surface_matrix.py --manifest
      docs/contracts/release/aggregation-manifest.n4a.json --lock
      docs/contracts/release/aggregation-lock.n4a.lock.json --matrix
      docs/contracts/release/public-v1-surface-matrix.n4a.json validate
  control_tests:
    status: passed
    command: python3 -m pytest -q
    result: 229 passed, 1 skipped (2026-09-01)
  dagml_data_lockstep_validation:
    status: passed_selected_pair_linux_binding_matrix_qualified_release_runtime_matrix_pending
    command: >-
      DAG_ML_REPO=/dev/shm/RC-v1-dagml-reconciled
      DAG_ML_DATA_REPO=/dev/shm/RC-v1-dmd-reconciled
      python3 scripts/validate_contracts.py
    evidence: >-
      The bidirectional contract validators pass on the clean selected pair
      Dag-ML 4988542a952c4ff20a0a70e210d1ba680a718985 (docs-only child
      of executable baseline 29abd30) and dag-ml-data
      bba0e2e891406077924634f8f39b55440e5875b0. The data workspace also
      passes format, workspace Clippy with warnings denied, exactly 272 tests
      with two explicitly ignored performance probes, Python 3.11-3.13,
      Node 20/22/24 WASM, Linux R and declared cross-compilation/package
      gates. Real non-Linux runtime, published native-provider artifacts,
      production providers and release-license closure remain required for
      LOCK-DATA.
locks:
  LOCK-ARCH: pending
  LOCK-REF: pending
  LOCK-METHODS: pending
  LOCK-DATA: pending
  LOCK-SAVE: pending
  LOCK-API: pending
  LOCK-STUDIO: pending
  LOCK-DROP: pending
  LOCK-RELEASE: baseline_lock_validated_current_train_pending_full_product_manifest_remote_advertisement
  LOCK-SOAK: pending
  LOCK-DOCS: pending
work_items:
  - id: ARCH-001
    owner: A
    dependencies: []
    state: complete
    input_sha: a34126d43c68e5122ba35a5ac8e2b36e4da56b61
    owned_files:
      - docs/contracts/release/migration-work-ledger.yaml
    acceptance_tests:
      - dag-ml/docs/adr/ADR-22-native-backend-v1-scope.md accepted
      - sphinx-build -W --keep-going -b html docs docs/_build/html
    review: Sol-xhigh ACCEPT after corrective re-review
    rollback: remove only the new Phase-0 control record; do not alter component contracts
  - id: ARCH-002
    owner: release-captain
    dependencies:
      - ARCH-001
    state: accepted_four_release_mapping
    input_sha: 4988542a952c4ff20a0a70e210d1ba680a718985
    owned_files:
      - docs/contracts/release/migration-work-ledger.yaml
      - dag-ml/docs/adr/ADR-24-v1-four-release-rollback-mapping.md
    acceptance_tests:
      - dag-ml/docs/adr/ADR-17-cutover-rollback.md inspected
      - dag-ml/docs/adr/ADR-24-v1-four-release-rollback-mapping.md accepted
      - selected Dag-ML contract validator passes after the docs-only ADR clarification
    review: >-
      User-authorized governance decision; independent release review remains
      pending. Dag-ML commit 4988542 clarifies that Rust owns Studio HTTP,
      WebSocket, scheduler, store and jobs while a bounded CPython
      library/plugin host remains permitted. LOCK-DROP means legacy
      orchestration is unreachable from strict product paths in R3; physical
      removal of the explicit engine=legacy rollback profile is separately
      governed after R4. The synchronized roadmap SHA-256 is
      52cf12c430378d5f2e038f3cb371b87332c87c2b0a41b6f03a4be7e22a4b58f5.
    rollback: supersede ADR-24 with an explicit release-count and rollback-policy replacement
  - id: REL-001
    owner: G
    dependencies:
      - ARCH-001
    state: inventory_complete_pending_integration_review
    input_sha: a34126d43c68e5122ba35a5ac8e2b36e4da56b61
    owned_files:
      - docs/contracts/release/migration-work-ledger.yaml
    acceptance_tests: []
    review: pending
    rollback: none; inventory is additive
  - id: REL-002
    owner: G
    dependencies:
      - REL-001
    state: baseline_lock_complete_current_train_regeneration_pending
    input_sha: a34126d43c68e5122ba35a5ac8e2b36e4da56b61
    owned_files:
      - docs/contracts/release/aggregation-lock.n4a.lock.json
    acceptance_tests:
      - release_lock_validation
      - release_surface_matrix_validation
    review: >-
      Clean selected-member validation passed after regeneration from the pinned,
      clean Core, DAG-ML, DAG-ML-data, Formats, IO, Datasets and Methods heads:
      release lock validation, native capability-ledger validation, and public
      surface-matrix validation all passed. Independent root review repeated
      those three selected-workspace commands and
      `python3.11 -m pytest -q tests/test_release_lock.py
      tests/test_native_capability_ledger.py tests/test_release_surface_matrix.py`
      on the published lock; the structural release/ledger/surface suite passed
      68 with 1 skip. On 2026-08-31, the lock-pinned reconstructed-root
      validators were repeated and the expanded structural suite including
      `tests/test_cutover_state_gate.py` passed 73 with 1 skip. That immutable
      lock remains valid only as the prior seven-component baseline: its
      manifest contains Dag-ML, dag-ml-data, Methods, Formats, IO, Core and
      Datasets, but not the full Python facade, Studio, Web or Tools. The
      current selected Core, Python, Studio, Web, Dag-ML, dag-ml-data, Formats
      and Tools SHAs are not contained by any locally
      known remote ref. Therefore the active train must not reuse or merely
      relabel this lock; it requires an expanded manifest, advertised commits
      and a clean reconstruction before LOCK-RELEASE can advance.
    rollback: retain current immutable lock; create a new validated lock only from selected release heads
  - id: PAR-001
    owner: G
    dependencies:
      - ARCH-001
    state: strict_dual_orchestration_oracle_selected_in_current_python_rc
    input_sha: 71dce9d9ecfc9e872393b96f19cae8aded34df41
    owned_files: []
    acceptance_tests:
      - nirs4all pytest -q tests/unit/api/test_dual_run_oracle.py
      - native real-probe checks finite CV/fold OOF metrics before temporary legacy execution
      - wheel-installed compatibility ledger resolves outside the checkout
      - compatibility-ledger NaN and positive or negative Infinity tolerances refuse before comparison
    review: >-
      Sol-xhigh ACCEPT: strict native-first orchestration dual-run compares ledger-bounded
      CV/fold metrics, winner/splits/OOF evidence and fails closed on missing/non-finite data.
      Legacy output is isolated in a temporary workspace. The exposed PLS operator remains
      Python sklearn-hosted; this item proves orchestration parity only, not Methods/libn4m
      numerical execution, which remains tracked by HPO/native API integration. The accepted
      historical evidence commits bd7fba45/dd93e9f6 were minimally reconciled
      onto the selected 6565c8ed lineage without cherry-picking its obsolete
      posture. Corrective descendants reject non-finite ledger tolerances and
      publish the already-supported native engine in the machine-readable
      registry. Current Python RC 71dce9d9 selects this result. Independent
      audit executed the real in-process dual probe, inspected an exact wheel
      containing the ledger and approved promotion; 520 API/registry tests,
      Ruff and targeted Mypy pass. No publication or remote advertisement is
      claimed.
    rollback: remove only the dual-run surface/tests/docs; preserve Python 0.12.0 oracle and existing fixtures
  - id: CAP-001
    owner: capability-ledger
    dependencies:
      - ARCH-001
      - PAR-001
    state: complete
    input_sha: a34126d43c68e5122ba35a5ac8e2b36e4da56b61
    owned_files:
      - docs/contracts/release/native-capability-ledger.v1.json
      - docs/contracts/release/native-capability-ledger/v1.schema.json
      - scripts/n4a_native_capability_ledger.py
      - tests/test_native_capability_ledger.py
      - tests/fixtures/native-capability-ledger/
      - .github/workflows/native-capability-ledger.yml
      - docs/contracts/release/migration-work-ledger.yaml
    acceptance_tests:
      - python3 -m pytest -q tests/test_release_lock.py tests/test_native_capability_ledger.py
      - python3 scripts/n4a_release_lock.py --workspace-root <selected-release-members> validate --manifest docs/contracts/release/aggregation-manifest.n4a.json --lock docs/contracts/release/aggregation-lock.n4a.lock.json
      - python3 scripts/n4a_native_capability_ledger.py --workspace-root <selected-release-members> validate
      - native-capability-ledger GitHub Actions workflow checks out lock-pinned selected members before validation
    review: "Sol-xhigh ACCEPT (CAP R7): lock-pinned workspace simulation passed 60 tests; local structural suite passed 59 with one selected-workspace skip. Global remote-SHA fetchability audit remains unproven and is not credited by this item."
    rollback: remove only CAP-owned ledger/schema/validator/tests/fixtures/CI additions; do not alter the immutable lock, manifest, or surface matrix
  - id: DATA-001
    owner: C
    dependencies:
      - ARCH-001
      - CAP-001
    state: complete
    input_sha: 16c1efdfcd98b3861c550b6609a69a9995793301
    owned_files:
      - nirs4all-io/crates/nirs4all-io-core/src/materialize/
      - nirs4all-io/crates/nirs4all-io-dagml/src/lib.rs
      - nirs4all-io/bindings/python/
      - nirs4all-io/bindings/wasm/
      - nirs4all-io/docs/VERSIONING.md
      - nirs4all-io/docs/DATASET_CONFIGURATIONS.md
    acceptance_tests:
      - cargo fmt --all --check; cargo clippy --workspace --all-targets -- -D warnings; cargo test --workspace
      - Python 3.11 MVP 235 passed, binding wheel 33 passed, ruff and mypy passed
      - Python and WASM metadata/test/build run locked and offline; wasm-pack release-style build passed
      - cross-language DatasetPackage v3 golden and fold sample-leakage preflight/emission probes passed
    review: Sol-xhigh ACCEPT. Node WASM smoke is not proven locally because Node is absent; CI retains that smoke as a platform gate.
    rollback: revert the IO-owned v2 assembled/v3 package wire changes and their readers/goldens together; no source data is mutated.
  - id: DATA-003
    owner: C
    dependencies:
      - DATA-001
      - CAP-001
    state: native_provider_linux_autonomous_wheel_qualified_cross_platform_runtime_matrix_pending
    input_sha: ed75da9ea58865c185d9db3ace0537956bd01357
    owned_files:
      - dag-ml-data/crates/dag-ml-data-core/
      - dag-ml-data/crates/dag-ml-data-provider/
      - dag-ml-data/crates/dag-ml-data-capi/
      - dag-ml-data/crates/dag-ml-data-wasm/
      - dag-ml-data/docs/contracts/
      - dag-ml-data/scripts/validate_contracts.py
      - dag-ml-data/scripts/smoke_wasm_provider_bindings.cjs
      - dag-ml-data/scripts/smoke_python_provider_wheel.py
      - dag-ml-data/scripts/test_provider_wheel_dirty_refusal.py
      - dag-ml-data/scripts/validate_release_metadata.py
      - dag-ml-data/.github/workflows/ci.yml
      - dag-ml-data/docs/STATUS.md
      - dag-ml-data/docs/SUPPORTED.md
    acceptance_tests:
      - branch views by_source, by_metadata, by_tag and closed by_filter preserve relation identity and reject unknown or unconstrained selectors
      - feature fusion and collation preserve source layout, repetition identity, masks and stable release of data and child-view handles
      - Cargo workspace fmt, Clippy with warnings denied and tests pass with exactly 272 passed and 2 documented performance probes ignored
      - C header/link, provider-vtable and Python ctypes smoke tests execute against the real C ABI
      - CPython 3.11, 3.12 and 3.13 build/install the exact abi3 Dag-ML and dag-ml-data wheels; the pure ctypes provider wheel plus four pytest cases and the Dag-to-Data integration pass on all three
      - Node 20, 22 and 24 execute default Dag/Data, Web, tarball, cross-repository and opt-in provider runtime smokes; provider lifecycle covers materialize, by_source, targets, typed f64 projection, collation, release and use-after-release refusal
      - R 4.1.2 builds, installs, loads and executes the native package on Linux; R CMD check has zero errors and one explicitly open non-standard-license warning
      - Data workspace plus Dag core, C ABI and WASM cross-compile for linux-aarch64, macOS x64/arm64 and Windows MSVC without claiming execution on those targets
      - dag-ml-data bba0e2e validates against selected Dag-ML 4988542 and the same Dag-ML validates back against this exact dag-ml-data checkout
      - release metadata validates as dag-ml-data 0.2.9; all seven crates package and the seven-crate publish-plan dry-run passes
      - the platform-specific Python provider wheel embeds the C ABI cdylib under the package, verifies a closed SHA-256, size, toolchain, target and clean forty-character source-commit manifest, and never discovers environment, sibling, Cargo or system libraries
      - a clean Python 3.11 venv installs the wheel without PYTHONPATH or LD_LIBRARY_PATH and executes materialize, all four branch views, identity/targets/features/tensor/fusion, release invalidation and destroy
      - missing or one-byte-tampered libraries fail with typed pre-ABI errors; tracked and untracked source dirtiness refuse release builds, while two canonical builds are byte-identical
      - canonical Linux wheel SHA-256 is 65de31eaa0fa4bd944e9dd5aad9abc9069beb72c07299da35254ef90773726cb and its bundled cdylib SHA-256 is 89e1ba9ca31e95fc2670e2b161d513a5520b06bb857bf1a128bcf22d63f9855a
    review: >-
      A dirty historical `RC-v1-dmd` worktree remains untouched and rejected
      as evidence. Canonical commits d5810ed through ed75da9 preserve the
      earlier Rust/C ABI/WASM matrix and add a reproducible autonomous Linux
      provider wheel. Independent audit found and closed an initial provenance
      hole where untracked sources were ignored, then rebuilt and smoked the
      exact canonical artifact. CI now contains concrete macOS arm64 and
      Windows x64 wheel lanes, but only Linux manylinux_2_34_x86_64 has local
      runtime evidence. LOCK-DATA remains open: the wrapped provider is still
      the in-memory conformance implementation rather than domain storage,
      no real macOS/Windows/aarch64 runtime was executed, no autonomous sdist
      is promised, and selected DMD/Dag SHAs are not advertised by remote refs.
    rollback: select the previous advertised dag-ml-data ref without modifying the dirty historical RC worktree or any user data.
  - id: IO-XLG-001
    owner: C + G
    dependencies:
      - DATA-001
    state: rust_python_wasm_c_abi_identity_qualified_r_and_matlab_runtime_pending
    input_sha: b9fc9307fdc3818060ec605007835efb5ff6c817
    owned_files:
      - nirs4all-io/tests/cross_binding/
      - nirs4all-io/bindings/wasm/src/lib.rs
      - nirs4all-io/bindings/wasm/tests/node_smoke.cjs
      - nirs4all-io/bindings/wasm/types/nirs4all-io.d.ts
      - nirs4all-io/bindings/matlab/
      - nirs4all-io/docs/IO_XLG_QUALIFICATION.md
      - nirs4all-io/.github/workflows/cross-binding.yml
    acceptance_tests:
      - the strict runner emits exactly one passed, refused or unavailable row for Rust, Python, WASM, R, MATLAB/Octave and direct C ABI, and overall_complete is true only for six passed rows
      - Rust CLI, an extracted abi3 CPython 3.11 wheel without editable install or PYTHONPATH, the produced WASM module under Node 22 and a compiled direct C probe reproduce one identity-rich assembled-summary-v2 golden byte-for-byte
      - the frozen witness preserves sample, observation, repetition, group, source, partition and fold provenance through the same Rust assembler on every qualified surface
      - local R and MATLAB/Octave absence remain explicit unavailable dispositions and keep overall_complete=false; the strict command exits nonzero unless incomplete inventory is explicitly requested
      - the qualification runner performs no dependency download; CI pins jsonlite 1.8.9 by SHA-256 before invoking the strict R lane
      - Rust workspace passes 192 tests, fmt, Clippy with warnings denied and no-default-features build; qualification tests pass 3 with Ruff, Mypy and workflow parsing
    review: >-
      IO commit b9fc930 adds an executable six-surface matrix without changing
      the frozen DATA schema or assembled-summary version. Independent report
      ec94a6a5be9e1159be4743de7ab1cfd4a61f7451d71f5e1c627e7d25a461c59f
      records Rust, Python, WASM and C ABI as passed and R plus MATLAB/Octave
      as unavailable, so the gate remains incomplete. The runner cannot install
      packages or silently skip a surface; CI provisions the exact jsonlite
      archive separately after digest verification. The broader pre-existing
      WASM smoke still fails its sample-id metadata assertion on the clean
      parent 98aca515; the new rich-witness parity test passes and does not hide
      or relabel that baseline defect. Real R and MATLAB/Octave execution and
      repair of the parent smoke remain required before LOCK-DATA can close.
    rollback: remove only the additive qualification harness, binding summary facades and CI job; retain the DATA contracts and never alter user datasets.
  - id: FMT-001
    owner: C + G
    dependencies:
      - DATA-001
    state: complete_real_legacy_and_studio_corpus_qualified
    input_sha: c8c5f32e2ca8b69bb8f94e0e91197b1043e44eeb
    owned_files:
      - nirs4all-formats/crates/nirs4all-formats/
      - nirs4all-formats/bindings/python/
      - nirs4all-formats/tests/conformance/
      - nirs4all-formats/tests/reverse_lab/
    acceptance_tests:
      - local legacy corpus classifies 219 entries as 207 parity and 12 expected refusals or auxiliaries with zero unnamed blocker
      - linked real Studio Hardness_0.8 dataset classifies all seven business files as three exact spectral-table parities and four expected metadata/target-only refusals
      - PerkinElmer, MicroNIR, FOSS and yamtot full-array comparisons preserve axes, values and sample identity within the declared source-export precision
      - Rust workspace fmt, Clippy with warnings denied and 292 tests pass with the real corpus present
      - Python binding and reverse-lab pass 24 tests with 3 documented skips; an abi3 wheel installs outside the checkout and reads a real MicroNIR file
      - external-reference conformance passes 52 tests with 31 documented environment or reference-reader skips and no failure
    review: >-
      The selected Formats RC fast-forwards from 181946f through the published
      v0.2.7 source 9ddb8f5 to c8c5f32. The only qualification correction is
      in the conformance harness: jcamp 1.3.2 exposes `JCAMP_reader` while older
      supported releases expose `jcamp_readfile`; accepting both restored 15
      real JCAMP comparisons instead of false skips. The source inventories are
      provenance-labelled local evidence and no corpus, private dataset or build
      output is committed. PerkinElmer covered 31 binary/CSV pairs (exact axes,
      maximum value delta 4.9999e-5), MicroNIR covered 120 SAM records against
      CSV (maximum value delta 5e-8 and axis delta 4.97e-4 from rounded export),
      FOSS covered three CAL/TXT pairs (exact axes, maximum delta 5e-8), and
      yamtot covered 16 NIR records bit-exact by sample identity and occurrence.
      The linked Studio matrices Xtrain 304x949, Xtest 80x949 and XY 384x949
      matched exactly. Missing Rscript and platform-only binding jobs remain
      documented release-platform gates; they do not hide an unclassified file
      in the available legacy or Studio corpus.
    rollback: revert only the conformance-harness compatibility commit; retain the immutable corpus classifications and never copy local corpus data into the repository.
  - id: MTH-001
    owner: B
    dependencies:
      - ARCH-001
      - CAP-001
    state: complete
    input_sha: 4d0cc0a9ac0348bca8a81db150b38417a9e410f3
    owned_files:
      - nirs4all-methods/Cargo.toml
      - nirs4all-methods/Cargo.lock
      - nirs4all-methods/bindings/rust/n4m/
      - nirs4all-methods/.github/workflows/rust-binding.yml
    acceptance_tests:
      - C ABI dev-debug build and CTest passed
      - Rust stable and MSRV 1.77 locked test/clippy/fmt passed; N4MM/N4MOPT corruption, ownership and ABI layout probes passed
      - Linux binding link/runtime behavior passed; macOS and Windows remain CI platform gates, not local evidence
    review: Sol-xhigh ACCEPT technical (Linux); follow-up Sol-high ACCEPT reconciled README status and scopes prediction parity to the committed four-backend matrix. macOS/Windows remain CI platform gates, not local evidence.
    rollback: remove the new Rust crate/CI only; preserve the C ABI and archived bindings.
  - id: MTH-003
    owner: B + G
    dependencies:
      - ARCH-001
      - MTH-001
    state: complete
    input_sha: 4d0cc0a9ac0348bca8a81db150b38417a9e410f3
    owned_files:
      - nirs4all-methods/docs/_extras/generate_v1_capability_matrix.py
      - nirs4all-methods/docs/_extras/test_v1_capability_matrix.py
      - nirs4all-methods/docs/architecture/v1_estimator_stateful_preprocessor_matrix.schema.json
      - nirs4all-methods/docs/architecture/v1_estimator_stateful_preprocessor_matrix.json
      - nirs4all-methods/docs/architecture/v1_estimator_stateful_preprocessor_matrix.md
    acceptance_tests:
      - deterministic matrix regeneration/check and focused tests passed (9 matrix + 3 doc-lint)
      - Draft 2020-12 schema independently validates the generated matrix
      - 37 catalog models (36 native plus moment_stack adapter), generic n4m_model_fit routes, and all fitted stateful preprocessor symbols are fail-closed against Linux/macOS/Windows ABI snapshots
    review: Sol-high ACCEPT. This is a code-surface capability proof, not numerical/runtime parity; stateless components and n4m_pipeline are explicitly out of the selected fitted-preprocessor scope.
    rollback: remove only the generated matrix, schema, generator and targeted tests; never alter C ABI symbols, catalog sources, or the Rust binding.
  - id: MTH-002
    owner: B
    dependencies:
      - MTH-001
      - MTH-003
    state: complete
    input_sha: 4d0cc0a9ac0348bca8a81db150b38417a9e410f3
    owned_files:
      - nirs4all-methods/Cargo.toml
      - nirs4all-methods/Cargo.lock
      - nirs4all-methods/bindings/rust/n4m/
      - nirs4all-methods/.github/workflows/rust-binding.yml
      - nirs4all-methods/docs/about.md
      - nirs4all-methods/docs/dev/DISTRIBUTION.md
    acceptance_tests:
      - Rust exposes C-ABI backed model fit/predict/N4MM and safe HPO ask/batch/tell/intermediate/best/trials/N4MOPT replay plus selection-only finetune
      - stable and MSRV 1.77 Rust gates, CTest and exhaustive ABI probe passed; package contains the two canonical license texts
      - sanitizer and Windows MSVC workflow paths are concrete and guarded in CI
    review: Sol-xhigh ACCEPT. macOS/Windows and sanitizer execution are CI gates not locally run; Rust remains official pre-release and outside cross-language prediction parity until a committed fixture exists.
    rollback: remove only the Rust binding/CI/docs additions and retain the C ABI, native test suite and archived bindings.
  - id: HPO-001
    owner: B + E
    dependencies:
      - MTH-002
      - DATA-001
      - PROV-001
    state: evidence_accepted_rc_reconciled_overlay_free
    input_sha: 29abd300e518d152eb5453ea57f5232dd2b595af
    owned_files:
      - dag-ml/crates/dag-ml-core/src/hpo.rs
      - dag-ml/crates/dag-ml-core/src/training_runtime.rs
      - dag-ml/crates/dag-ml-core/src/runtime/
      - dag-ml/crates/dag-ml-core/src/bundle.rs
      - dag-ml/crates/dag-ml-core/src/replay.rs
      - dag-ml/crates/dag-ml-py/Cargo.lock
      - dag-ml/scripts/validate_release_metadata.py
      - dag-ml/docs/HPO_METHODS_ADAPTER.md
    acceptance_tests:
      - native Methods ask/batch/tell/intermediate/best/trials/failure/prune with TPE and pruner
      - N4MOPT durable checkpoint/restart trajectory and archive persistence/replay
      - controller-owned execution with attested DataBinding/identity, DAG folds/OOF/selection/refit
      - N4MM package reload into a fresh controller followed by predict without labels
      - extracted default dag-ml package has no sibling dependency; any local Methods overlay is explicit and fail-closed
      - selected Dag-ML RC descends from the accepted HPO/results/conformal lineage, deletes the sibling manifest overlay, and resolves its standalone Python binding with --locked n4m 0.1.1
      - overlay-free real Methods gate passes 652 tests with 2 unrelated ignores; full workspace fmt/clippy -D warnings/tests, release/contracts validators and wheel smoke pass
    review: >-
      Independent review ACCEPT. The controller-owned native Methods HPO
      implementation was reviewed at Dag-ML
      e560a8ffc2da5fd489f65c010746492d1ef1b39b and remains byte-identical in
      the documentation-only descendant
      7b0111981071cfc9b509c1eea100f0430372f103. Methods source
      aabdecfdd76d1a4d12cfbbade4eeee0d30a6ea47 produced the explicit
      FITPACK-free ABI 2.2.0 libn4m runtime; the published dynamic Rust binding
      is n4m 0.1.1. Methods CTest passed 2/2. The feature-local Dag-ML gate
      passed clippy, 610 library tests with only two unrelated performance
      probes ignored, 5 conformal tests, and 37 training-runtime integration
      tests. Focused native evidence passed random HPO, TPE/Median pruning,
      COMPLETED/PRUNED/FAILED terminal states, intermediate feedback, exact
      N4MOPT checkpoint/resume trajectory, deterministic selection plus
      exactly-once refit, and fresh-controller target-free N4MM replay.
      Missing N4M_LIBRARY_PATH fails with status 2; no HPO test skips when
      Methods is absent. The packaged crate excludes the local overlay and
      resolves n4m 0.1.1 and Core 0.3.15 from registries only. Commit
      7b0111981071cfc9b509c1eea100f0430372f103 removes the stale
      pre-publication feature claim and adds a fail-closed release-metadata
      guard tied to Cargo.toml. Commit ff8ab3a reconciles the selected RC onto
      that lineage, removes Cargo.toml.methods-local, adds a standalone Python
      Cargo.lock SHA-256 22bf9a1ed2d05cf35b8df10c7c0e8f9bdd702b1848c75887435440a596098eeb,
      and enforces n4m 0.1.1 plus --locked wheel builds in CI. The resulting
      0.3.18 abi3 wheel SHA-256 is
      db3394a4e2ea74b40038235e807e5f2b9027fc9eaf94557040e20216897c86dc;
      the real Methods ABI 2.2.0 library SHA-256 is
      ad68e6c83c19c50fcfc859c14e9c51ea333e307eaa254040d62afa911a20635a.
      Commit 29abd30 carries the accepted Studio CPython plugin-host boundary
      onto the same selected lineage without restoring a Python orchestration
      backend. This accepts HPO-001 evidence only:
      LOCK-METHODS and LOCK-API remain pending, API-001/API-004 retain their
      remaining acceptance work, and no R2 waypoint or native-default flip is
      closed by this review.
    rollback: select the archived pre-reconciliation ref without deleting accepted HPO/results commits; preserve Methods ABI, packs and existing DAG contracts.
  - id: CONF-001
    owner: E + F
    dependencies:
      - DATA-001
      - PROV-001
      - STORE-001
    state: core_archive_replay_e2e_qualified_studio_transport_pending
    input_sha: e0fd8021104b111a61c270671cee0529824c1aff
    owned_files:
      - dag-ml/crates/dag-ml-core/src/conformal.rs
      - dag-ml/crates/dag-ml-core/src/bundle.rs
      - dag-ml/crates/dag-ml-core/src/replay.rs
      - dag-ml/crates/dag-ml-core/src/training_runtime.rs
      - nirs4all-core/bindings/rust/nirs4all/src/archive_v1.rs
      - nirs4all-core/bindings/rust/nirs4all/src/archive_v2.rs
      - nirs4all-core/bindings/rust/nirs4all/src/native_methods_replay.rs
      - nirs4all-core/bindings/python-native/src/lib.rs
      - nirs4all-core/bindings/python/src/nirs4all_core/_archive.py
      - nirs4all-ui/src/conformal/
      - nirs4all-studio/src/components/predictions/viewer/conformalChartData.ts
      - nirs4all-studio/sidecar/src/conformal_store.rs
    acceptance_tests:
      - native split-conformal calibration, coverage artifact and interval application preserve Rust goldens and sample identity
      - native calibration to archive to process-close to Core load to predictor replay to interval E2E passes without importing Python conformal runtime
      - public ABI/binding exposes native calibration/apply/load and archive persistence is integrity checked
      - UI consumes the stored artifact and refuses sample_id ordering mismatch without any statistical recalculation
      - a clean autonomous Core wheel reopens the calibrated Archive V2 in a fresh isolated child process and returns the exact closed ConformalPresentationV1 without importing a Python conformal runtime
      - Archive V2 tamper and invalid contracts refuse before process-global libn4m configuration; Core delegates interval composition and identity/order validation to DAG-ML
      - two clean wheel builds are byte-identical at SHA-256 1006a5d96233bd457e95b19bcb3db38f09c3160d2c645e967c3520b7e34e14bd
    review: >-
      Two independent Sol-xhigh reviews ACCEPT the DAG-ML V2 runtime: calibration is
      PREDICT-only, relation-authoritative, distinct and identity-disjoint from development;
      Package/Replay/Outcome V2 schemas are closed, V1 fields (including raw null) refuse,
      and loaded-package replay recomputes exact interval bounds. Re-signed relation,
      origin, source/replay, output, interval deletion/injection and transplant probes refuse.
      W1 closure also pins conformal.rs and conformal_runtime.rs. The bounded Core archive-view
      remains ACCEPT metadata-only. A separate Sol-xhigh review ACCEPTS the Studio chart gate:
      stored intervals attach only when explicit wire sample IDs match exact row order; numeric
      positional indices never impersonate stable IDs. Native archive execution and the shared
      Core-to-UI artifact transport E2E required by this cross-component item are still pending.
      RC Studio commit `8ce8357` adds a dependency on the published `nirs4all 0.3.22` aggregate
      and a library-only `ConformalPresentationStore`: it atomically persists a self-validating
      `ConformalPresentationV1` by fingerprint, and refuses tampered bytes or malformed keys on
      re-read. This is persistence evidence only: no HTTP ingestion route, data-envelope adapter,
      native Core replay invocation, or Core-to-UI E2E claim has been added.
      Historical Core commit c17d49e contained the intended aggregate API but
      was not an ancestor of the selected Core lineage. Canonical commit
      e0fd802 ports the minimal composition onto the current Archive V2 and
      DAG-ML 0.3.18 replay surfaces with explicit provenance. A fresh wheel-only
      CPython 3.11 child reopened the archive after producer-process closure and
      returned the exact package-owned conformal presentation; tamper refused
      before libn4m. Rust workspace tests pass 63 plus one doctest, Python passes
      97 with five explicit live-environment skips, and the wheel is
      byte-reproducible. Qualification is local Linux x86_64; manylinux,
      macOS/Windows and the final Core-to-Studio-to-UI transport remain open.
    rollback: disable the additive native conformal capability and retain the R1 legacy engine; never rewrite legacy archives.
  - id: WEB-001
    owner: F + G
    dependencies:
      - CAP-001
      - DATA-003
    state: strict_wasm_product_profile_qualified_transitional_profile_explicit
    input_sha: 4fcd43debdb2678d447023cc44829505a251163a
    owned_files:
      - nirs4all-web/web-app/src/engine/web-profile.ts
      - nirs4all-web/web-app/src/engine/main-engine.ts
      - nirs4all-web/web-app/src/engine/dagml-engine.ts
      - nirs4all-web/web-app/src/engine/worker.ts
      - nirs4all-web/web-app/src/engine/client.ts
      - nirs4all-web/web-app/src/engine/client.singlefile.ts
      - nirs4all-web/web-app/src/engine/fixtures/core-parity/
      - nirs4all-web/web-app/scripts/validate-web-profile.mjs
      - nirs4all-web/web-app/scripts/run-smokes.mjs
      - nirs4all-web/web-app/tests/rt-fallback-smoke.mjs
      - nirs4all-web/web-app/vite.config.ts
      - nirs4all-web/web-app/package.json
      - nirs4all-web/.github/workflows/web-ci.yml
      - nirs4all-web/.github/workflows/deploy-pages.yml
    acceptance_tests:
      - the standard production build and explicit strict build select strict-wasm; development, test, transitional and single-file modes remain visibly separate profiles
      - strict-wasm refuses allowFallback true, direct/offline runners, JavaScript model execution, provider-to-in-memory matrix degradation and scheduler fallback at request, engine and worker boundaries
      - the product remains client-side only and permits only a healthy portable Core/WASM plus Dag-ML, libn4m and provider chain; remote compute is not introduced
      - every build emits nirs4all-web-profile.v1.json and validation fails before preview when the requested profile differs from the artifact manifest
      - Core parity oracle and four pipeline fixtures are vendored with exact source SHA-256 fc008911607a3260218d98fee86ec38e4dd0e5994e91126f7403baff5bc7377c, so isolated worktrees execute rather than silently skip parity
      - Node 22 full Vitest passes 178 tests, strict-profile Vitest passes 20, TypeScript passes, and the ABI catalogue proves 64 referenced symbols against 733 exports plus five Studio DAG operators
      - standard, strict, transitional and single-file builds pass; strict browser smoke proves allowFallback true is refused and transitional smoke proves the historical fallback remains typed, finite and explicitly diagnosed
      - a strict artifact requested as transitional exits 2 before starting a preview server
    review: >-
      Web commits 0154611, 415bc34 and 4fcd43d establish the strict product
      profile, remove parity-test dependence on sibling checkout topology, and
      make the browser fallback smoke profile-aware. The strict profile has no
      hidden JavaScript or scheduler degradation; transitional behavior remains
      available only through an explicit development/single-file build and
      artifact manifest. The full unit/type/build/catalog and strict plus
      transitional fallback gates pass on the selected clean Web worktree.
      Artifact-driven converted-prediction/performance smokes, the Python paper
      repository handoff and optional external format datasets remain separate
      release gates and are not masked or claimed by this item. Final staged
      WASM binaries must still be rebuilt from the future aggregation lock.
    rollback: revert the strict profile selector, manifest and profile-aware smoke commits together; preserve user browser storage and exported n4a artifacts.
  - id: UI-001
    owner: F + G
    dependencies:
      - WEB-001
    state: immutable_ui_tarball_producer_qualified_studio_web_pin_and_registry_publish_pending
    input_sha: b7cadebd0e2857c8e8a65d7e7b767a949fa60b1f
    owned_files:
      - nirs4all-ui/scripts/release-artifact.mjs
      - nirs4all-ui/scripts/release-artifact.test.mjs
      - nirs4all-ui/.github/workflows/release-npm.yml
      - nirs4all-ui/package.json
      - nirs4all-ui/README.md
    acceptance_tests:
      - the build job produces one nirs4all-ui tarball plus a closed provenance manifest and uploads both as one workflow artifact
      - the publish job downloads, verifies and publishes that exact tarball instead of rebuilding or publishing from a second source tree
      - the manifest pins package name and version, forty-character source commit, clean-tree state, byte size, SHA-256, npm SHA-1, SHA-512 integrity, entry count and required public files
      - dirty tracked source, non-empty output, symlink or non-file inputs, unknown manifest fields, source-commit mismatch and tampered tarball bytes fail closed
      - two clean builds from the selected commit produce byte-identical tarballs with SHA-256 7a232c746eeeda526a26d79ef79ab83f6b2363d2d8ec37d92e674ae61b248340
      - Node 24 typecheck, 282 Vitest tests, four artifact-contract tests, build, pack smoke and packed React 18/19 consumer smokes pass
    review: >-
      UI commit b7cadeb closes the release-producer half of UI-001. It makes
      the tarball the immutable handoff between build and npm publication and
      provides an offline verifier for downstream consumers. No registry
      publication or external push was performed. Studio and Web still point
      at their local vendor directories, so the same-version/same-hash
      consumer pin and their full product gates remain required before UI-001
      can be complete.
    rollback: revert only the additive artifact builder, verifier and workflow handoff; retain the UI package source and consumer vendor copies.
  - id: GATE-001
    owner: G
    dependencies:
      - PAR-001
      - CAP-001
    state: python_legacy_and_core_executed_web_and_studio_refusals_attested_four_way_perf_pending
    input_sha: a2598184c73284592634abcb0a6a7fbfa01bce8d
    owned_files:
      - nirs4all-benchmarks/src/nirs4all_benchmarks/qualification/contract.py
      - nirs4all-benchmarks/src/nirs4all_benchmarks/qualification/runner.py
      - nirs4all-benchmarks/src/nirs4all_benchmarks/qualification/json_result_adapter.py
      - nirs4all-benchmarks/src/nirs4all_benchmarks/qualification/scientific_runtime_adapter.py
      - nirs4all-benchmarks/src/nirs4all_benchmarks/qualification/python_legacy_adapter.py
      - nirs4all-benchmarks/src/nirs4all_benchmarks/cli.py
      - nirs4all-benchmarks/tests/test_artifact_qualification.py
      - nirs4all-benchmarks/tests/test_scientific_runtime_adapter.py
      - nirs4all-benchmarks/tests/test_python_legacy_adapter.py
      - nirs4all-benchmarks/tests/test_web_wasm_readiness.py
      - nirs4all-benchmarks/tests/test_studio_sidecar_readiness.py
      - nirs4all-benchmarks/docs/QUALIFICATION.md
      - nirs4all-benchmarks/docs/CLI.md
      - nirs4all-benchmarks/docs/perf001-artifact-inventory.v1.json
      - nirs4all-benchmarks/docs/perf001-scenario.v1.json
      - nirs4all-benchmarks/docs/perf001-web-wasm-readiness.v1.json
      - nirs4all-benchmarks/docs/perf001-studio-sidecar-readiness.v1.json
    acceptance_tests:
      - the manifest names Python oracle, Core/Rust, Studio sidecar and Web/WASM exactly once and pins every scenario, executable and input artifact by absolute path plus SHA-256
      - adapters execute in a temporary directory with a cleaned environment and cannot discover sibling checkouts or inject source trees through PYTHONPATH
      - every component reports version, forty-character commit SHA, artifact evidence, timings, metrics, observation digest and a terminal passed, refused or failed disposition
      - missing or digest-mismatched inputs refuse before execution; unfinished output, declared fallback, identity mismatch and numerical divergence never pass
      - an unavailable runtime launcher is represented as required and makes the overall gate refused; it is never silently skipped
      - the Core launcher accepts only runtime_python, Core, Dag-ML, PyYAML, Archive V2 and methods-library artifacts and verifies their parent and installed-child bytes before direct Core replay
      - a Core/native facade is explicitly refused when presented as the Python oracle; the selected oracle calls nirs4all.run with literal engine='legacy' and allow_fallback=false
      - the selected Core artifacts execute the two-row multi-target witness without fallback and return protein [1.6363636363636365, 2.4999999999999996] and moisture [13.272727272727273, 15.0]
      - the Python oracle verifies its CPython 3.11 runtime, all 44 installed wheels against their declared RECORD bytes and mandatory dependency closure, and imports only from the isolated runtime prefix
      - the Python oracle fits, exports, reloads and predicts the exact legacy PLS witness; Python and Core both pass with maximum absolute difference 2.870914617147946e-10 and zero observations outside tolerance
      - source checkout injection, native or dag-ml oracle substitution, fallback, outer artifact tamper and installed-byte tamper all refuse without becoming a skip
      - selected Web 4fcd43d proves its strict-wasm profile and six exact WASM digests but remains required/refused before browser launch because it exposes neither Archive V2 replay nor a single multi-target result contract
      - two independent Web mono-target fits are explicitly rejected as a substitute for the canonical Archive V2 multi-target operation
      - selected Studio f50a0bd verifies its content-addressed Rust sidecar and runtime contract, isolated CPython stdio preflight and exclusive Rust listener ownership but remains required/refused because the selected Python package exposes no studio_scientific_job_v1 callable and the Rust durable terminal bridge is absent
      - Studio refusal occurs before request-body parsing, workspace access, job allocation, event publication, durable writes or scientific execution; submission transport, a Python listener, fabricated JSON or direct Core replay outside Studio are rejected substitutes
      - Ruff passes, Mypy passes over 44 source files and the isolated checkout passes 129 tests; the Core launcher wheel remains byte-reproducible at SHA-256 806905011f1e01a43de26cf4ce89af6bb49e4819d64c0044039caa880fd4ffc4
    review: >-
      Benchmarks commits b9be6bf, b21f35f and 063e2d3 add the executable stdio
      JSON protocol, strict four-runtime runner and the Core plus Python-legacy
      launchers.
      An independent detached-worktree rerun confirmed Ruff, source Mypy, all
      117 tests, the exact wheel hash and a real selected-artifact Core replay.
      Python remains an explicit legacy oracle rather than a native facade.
      An independently rerun exact local report confirmed Python and Core as
      passed with the frozen multi-target witness; its 44-wheel closure and
      pyopls source-to-wheel provenance are content-addressed local evidence,
      not published or final-lock artifacts. Studio and Web are evidence-backed
      required refusals rather than executed lanes. The Studio scientific
      executor, missing Web replay surface, immutable artifact
      staging, four-way executed report and measured PERF-001 budgets remain
      open and cannot be represented as a skip or partial green gate.
      Commit 2b389cb then content-addresses the selected strict Web profile,
      six WASM payloads and source-contract evidence. It proves a required
      refusal rather than a runtime pass: Web has portable fit/predict only,
      no Archive V2 reader/replay and a single-target MaterializedDataset.
      Benchmarks passes 126 tests plus Ruff and Mypy; Web typecheck, strict
      tests and production build pass. A Web aggregate replay surface and the
      Studio scientific executor are still required before the four-way gate
      can execute. Commit a259818 then records the selected Studio f50a0bd
      release sidecar and runtime-contract digests plus a live Rust-only
      listener probe. Its bounded CPython stdio preflight is isolated and
      cannot bind a network socket, but nirs4all 0.10.3 has no callable
      studio_scientific_job_v1 and the Rust result/event/cancellation terminal
      bridge is absent. The lane therefore remains required/refused with
      executed=false; 129 Benchmarks tests, Ruff, Mypy and the targeted Studio
      Rust/Electron gates pass. This evidence is tied to f50a0bd and must be
      regenerated when a later Studio commit becomes selected.
    rollback: remove only the additive qualification package, CLI entry point, tests and documentation; retain historical benchmark records.
  - id: DOC-WEB-001
    owner: H + F + G
    dependencies:
      - REL-002
      - STU-001
      - MTH-DOC-003
    state: org_lock_driven_staging_complete_public_artifacts_and_cross_site_sync_pending
    input_sha: 427ffb34268955aa7a785479a9c7f4232aa77edb
    owned_files:
      - nirs4all-org/
      - nirs4all-cockpit/data/current.json
      - nirs4all-web/
      - nirs4all-studio/.github/workflows/
      - nirs4all-methods/.github/workflows/docs.yml
    acceptance_tests:
      - canonical versions, SHAs and artifacts are generated from the selected validated aggregation lock
      - strict HTML/link/accessibility/SEO and Web smoke gates pass from clean builds
      - Studio contract verifier is committed and run by CI; documentation claims only capabilities proven by the same release lock
      - Org release staging projects only public-safe version and source-SHA fields from a validated lock; absolute paths, worktrees and private URLs are rejected
      - the current incomplete R2 lock renders every component as pending with no download and falls back to Publication en cours for absent, invalid or incomplete manifests
      - the public projection is byte-reproducible with SHA-256 3868dbe836cba25d5b681864fbbffb8178c011353800ad1310767ae461f48f32
      - static validation covers manifest, HTML, internal links, accessibility, SEO, JSON-LD and sitemaps; eight negative tests, Ruff, Mypy, HTTP and headless Chrome smokes pass
    review: >-
      Org commits 6899cfe and 427ffb3 replace divergent hard-coded release
      claims on the touched landing/tools surfaces with a closed lock-derived
      staging manifest and strict CI validation. Independent reproduction and
      browser fallback smokes passed. The selected lock currently proves only
      seven source selections and no public artifact, so the page deliberately
      says R2 is in progress and exposes no download. Cockpit, Studio/Web and
      Methods cross-site synchronization, public artifacts, publication and
      deployment remain open; no R1/R2/R3/V1 announcement is eligible yet.
    rollback: revert generated public content and deployment manifests atomically; no release, site deployment or public announcement without authorization.
  - id: DOC-WEB-001B
    owner: H + F
    dependencies:
      - DOC-WEB-001
      - REL-002
    state: cockpit_lock_driven_staging_complete_publication_and_cross_site_sync_pending
    input_sha: 669cc81ceb4eebfc1b5249dec20fb91caa3ac28a
    owned_files:
      - nirs4all-cockpit/.github/workflows/ci.yml
      - nirs4all-cockpit/cockpit/release_staging.py
      - nirs4all-cockpit/data/release-staging.json
      - nirs4all-cockpit/docs/release-staging.md
      - nirs4all-cockpit/ops/release-staging/
      - nirs4all-cockpit/scripts/release_staging.py
      - nirs4all-cockpit/tests/test_release_staging.py
    acceptance_tests:
      - Cockpit release staging consumes only explicit validated manifest and lock inputs and emits a deterministic public-safe R2 projection
      - the current incomplete lock remains R2 in progress, exposes no artifact or download and cannot be represented as a complete or published release
      - absolute paths, worktree paths, private URLs, duplicate keys, incomplete component selections and projection drift are rejected
      - direct CLI execution from an external directory resolves the selected checkout rather than a stale editable cockpit package
      - the clean selected checkout passes 164 tests, Ruff, Mypy, target validation, Node syntax validation and the headless Chrome DOM smoke
    review: >-
      Cockpit commits b90d9be and 669cc81 add the separate closed
      n4a.cockpit-release-staging/v1 projection without changing the existing
      public snapshot or dashboard application. An independent rerun confirmed
      all 164 tests and Ruff; the hermetic CLI regression now passes even with
      an unrelated stale editable package. The projection remains staging-only:
      cross-site synchronization, public artifacts, publication and deployment
      are still open and no release announcement is eligible.
    rollback: remove only the additive release-staging projection, CLI, tests and CI gate; retain the existing public cockpit snapshot and dashboard.
  - id: MTH-DOC-001
    owner: H + B
    dependencies:
      - MTH-003
    state: complete
    input_sha: f69a2ec7634f3e0b08d1b6ec01ceceaae368967a
    owned_files:
      - nirs4all-methods/catalog/bibliography/
      - nirs4all-methods/catalog/scripts/*bibliography*.py
      - nirs4all-methods/catalog/tests/test_bibliography_catalog.py
      - nirs4all-methods/docs/bibliography.md
      - nirs4all-methods/docs/_static/bibliography.*
    acceptance_tests:
      - 73 legacy citations remain byte-exact and 15 reviewed finetuning entries are proven from Git-pinned blobs
      - 14 bibliography tests, reproducible migration/generation, strict catalog checks and scientific provenance validation passed
    review: Sol-xhigh ACCEPT. No structured fact was inferred for legacy citations; reviewed records are source/digest-bound.
    rollback: remove bibliography v1 additions and generated artifacts together; retain legacy methods_bibliography.py and existing reader behavior.
  - id: MTH-DOC-002
    owner: H + B
    dependencies:
      - MTH-DOC-001
    state: complete
    input_sha: fe5c077ba7ddccacb3befce6dbc9e398458b20d3
    owned_files:
      - nirs4all-methods/catalog/schema/bibliography_v1.json
      - nirs4all-methods/catalog/scripts/migrate_bibliography.py
      - nirs4all-methods/catalog/scripts/validate_bibliography.py
    acceptance_tests:
      - v1 branches unstructured legacy and reviewed structured records with closed Draft 2020-12 validation
      - Git-revision/blob/entry-digest tampering and shallow-history diagnostics refuse fail-closed
    review: Sol-xhigh ACCEPT.
    rollback: reverse schema/migration/validation and generated v1 catalog as one unit; do not modify source bibliography facts.
  - id: MTH-DOC-003
    owner: H + B + G
    dependencies:
      - MTH-DOC-002
    state: complete
    input_sha: fe5c077ba7ddccacb3befce6dbc9e398458b20d3
    owned_files:
      - nirs4all-methods/docs/bibliography.md
      - nirs4all-methods/docs/_static/bibliography.bib
      - nirs4all-methods/docs/_static/bibliography.links.json
      - nirs4all-methods/docs/_static/nirs4all-methods.cff
      - nirs4all-methods/.github/workflows/catalog-validate.yml
    acceptance_tests:
      - generated BibTeX has 88 valid entries and passes bibtex; reviewed provenance/conditional DOI-URL links render in Sphinx
      - CI triggers on CFF and finetuning source and installs parser gate
    review: Sol-xhigh ACCEPT. Existing non-bibliography Sphinx warnings remain outside this lot.
    rollback: reverse generated documents/CI wiring with their generator; retain catalog source until a replacement migration is accepted.
  - id: STU-001
    owner: A + F
    dependencies:
      - ARCH-001
      - CAP-001
    state: complete
    input_sha: c1e1e5836aff0d3d2dcf81379204386e03dffa00
    owned_files:
      - nirs4all-studio/docs/contracts/studio-v1/
      - nirs4all-studio/scripts/verify-studio-v1-contract.py
      - nirs4all-studio/tests/test_studio_v1_contract.py
      - nirs4all-studio/package.json
    acceptance_tests:
      - physical route/OpenAPI/WS/readiness/jobs/cancellation/error/workspace snapshots validate in project FastAPI 0.128 and system FastAPI 0.139
      - focused contract suite passed 32/32; alternate runtime passed 31 with one expected portability skip
      - direct full backend equivalent passed 2334 with 59 skips; privacy scanner, exception ledger and schema mutation tests fail closed
      - RC restoration preserves every frozen route and OpenAPI property; the sole route addition is POST /api/workspace/migrate/report-preview and 61 properties are additive
      - restored conversion/dataset compatibility, concurrent lazy import and contract gates passed 74 focused pytest cases with one portability skip; TypeScript and 29 focused Vitest cases passed
    review: >-
      Sol-xhigh ACCEPT. This freezes the legacy FastAPI compatibility baseline only;
      it does not provide a Rust sidecar or selectable native Studio runtime. An
      independent RC re-review at c1e1e58 ACCEPTS zero removed paths/properties
      relative to the frozen b22c4c8 oracle, restored conversion/dataset fields,
      best-effort activation guards and serialized lazy imports. The unique new
      migration-preview route is additive. This acceptance does not close
      LOCK-STUDIO or any Rust-only cutover claim.
    rollback: remove only Studio contract fixtures/schemas/verifier/tests and package script; no route, Electron package, workspace or user-data migration was changed.
  - id: STU-002
    owner: F
    dependencies:
      - STU-001
    state: complete
    input_sha: dc1c78028eb93be173e27e4f3d86a97bfa27798a
    owned_files:
      - nirs4all-studio/sidecar/
      - nirs4all-studio/scripts/build-native-sidecar.cjs
      - nirs4all-studio/.github/workflows/ci.yml
      - nirs4all-studio/.github/workflows/release-unified.yml
    acceptance_tests:
      - Rust sidecar health/readiness/job/cancellation/WS protocol tests passed (15), including fragmented HTTP, bounded resources, WS upgrade validation and fixture correlations
      - release build and loopback readiness smoke passed; STU-001 frozen-contract verifier and targeted pytest passed
      - installer CI builds the target-specific sidecar after backend cleanup, verifies staged and unpacked binaries, and runs readiness smoke when runner architecture matches; Node/YAML structure and focused packaging tests pass
    review: >-
      Sol-xhigh ACCEPT. This is an opt-in R1 skeleton with no Python backend,
      scientific/store implementation, Electron switch, legacy route serving, or full-parity
      claim; legacy_route_parity is explicitly not_started. Follow-up RC commits `56ad25c`
      (shared UI shim sync) and `e860721` add only the Rust-owned `GET /api/system/build`
      inventory route: Electron passes fixed product metadata and explicit plugin-host runtime
      classification to the sidecar; Rust owns the HTTP response and uses the configured
      CPython host solely for a bounded isolated `torch` library probe (15 s, 8 KiB), never as
      an HTTP backend or scientific-job fallback. Sidecar unit/contract tests, the Studio native
      gate (30 tests), TypeScript/lint gates, and 3,726 frontend tests passed; the 2,335-case
      backend pytest equivalent also passed directly. The npm backend runner remains unavailable
      in this WSL UNC shell before pytest starts, so it is not release evidence. Follow-up RC
      commit `267ffde` moves the already-linked workspace catalogue, `POST
      /api/workspaces/:id/activate`, and `DELETE /api/workspaces/:id` to Rust. The Electron
      transport routes only those exact state mutations to the sidecar; linking, pruning,
      scanning, result discovery, and all workspace/scientific-store access remain legacy until
      their native scanner/store contracts are available. Rust tests (26 unit + 5 contracts),
      the native Studio gate (31 tests), lint and 3,727 frontend tests passed.
      Follow-up RC commit `964facc` routes `GET /api/system/network` to Rust without a Python
      plugin host. It derives the established `online`, `forced`, `mode`, and `env_forced`
      response solely from `NIRS4ALL_OFFLINE` and the bounded legacy-compatible
      `update_settings.yaml` preference location; missing, malformed, or oversized preferences
      safely resolve to `auto`. It neither performs a network probe nor starts a Python backend.
      Rust format/clippy, 28 unit tests, 5 contracts, and a Windows GNU target compile passed,
      alongside the native Studio gate (32 tests), lint, and 3,728 frontend tests.
      Follow-up RC commit `254e87f` connects the renderer's existing health check to the
      sidecar's already frozen `GET /api/health` V1 bootstrap contract. That route is fully
      native and needs neither an interpreter nor mutable product state; the native Studio gate
      (33 tests), lint, and 3,729 frontend tests passed. This makes no readiness, WebSocket,
      scientific-route, scanner, or store parity claim.
      Follow-up RC commit `16a988e` moves `GET /api/updates/version` to the Rust sidecar.
      Electron supplies the product version at launch; the configured CPython library host runs a
      bounded isolated inspection solely for the installed `nirs4all` version plus interpreter and
      platform facts. The HTTP route rejects an absent or failed plugin host rather than launching
      FastAPI or falling back to scientific execution. Rust format/clippy, 29 unit tests, 5
      contracts, and a Windows GNU target compile passed, alongside the native Studio gate (34
      tests), lint, and 3,730 frontend tests. Update checking, settings, downloads, runtime
      mutation, scanner/store, and all scientific routes remain legacy.
      Follow-up RC commit `6e09db6` moves `GET /api/updates/runtime/status` to the Rust
      sidecar when the explicit CPython library host is configured. Rust owns the HTTP response,
      reads bounded local `venv_metadata.json`, and calculates runtime size; the isolated host
      supplies only interpreter facts and a bounded (256 KiB, at most 4,096 entries) installed-
      distribution inventory. The contract test exercises that route against the configured
      host while scientific execution remains unavailable. Rust format/clippy, 30 unit tests, 5
      contracts, the configured-host integration test, and a Windows GNU target compile passed,
      alongside the native Studio gate (35 tests), lint, and 3,731 frontend tests. This does not
      add update mutation, scanner/store, or scientific-route parity; those remain legacy.
      Follow-up RC commit `d84d612` moves `GET` / `PUT /api/updates/settings` to the Rust
      sidecar without requiring a CPython host. Rust parses and atomically replaces the bounded
      legacy-compatible YAML preference file, preserves the PATCH merge shape, and rejects
      malformed values. Because update checking remains FastAPI during this transition, its
      manager detects an external atomic replacement and invalidates its cached GitHub release
      when the prerelease channel changes; the Rust-to-Python handoff test confirms an update
      takes effect without restart. Rust format/clippy, 31 unit tests, 5 contracts, the configured
      host integration test, a Windows GNU target compile, the handoff pytest, the native Studio
      gate (36 tests), lint, and 3,732 frontend tests passed. Checking, downloads, runtime
      mutation, dependencies, snapshots, scanner/store, and scientific routes remain legacy.
      Follow-up RC commit `6d4ee71` moves `GET /api/system/status` to the Rust sidecar without
      a Python host. It reads only the active linked-workspace catalogue record and reports it
      as catalogue state: it performs no path inspection, scan, dataset read, or WorkspaceStore
      access. The historical `nirs4all_available` field reports only whether an explicit Python
      plugin host was configured; the route neither imports nor executes Python. Rust
      format/clippy, 32 unit tests, 5 contracts, the native Studio gate (37 tests), lint, and
      3,733 frontend tests passed. Scanner/store and scientific-route parity remain legacy.
      Follow-up RC commit dc1c780 closes the packaging omission that allowed
      Windows, macOS and Linux installers to run electron-builder without a
      native sidecar. Authenticode, Apple signing and notarization remain
      external secret-backed release gates and are not simulated locally.
    rollback: remove the standalone sidecar directory only; Studio legacy FastAPI/Electron paths and user workspaces are untouched.
  - id: SAVE-001
    owner: D
    dependencies:
      - ARCH-001
      - CAP-001
    state: complete
    input_sha: 3097da5658d0abca575d36db1f6a82f1f5d55490
    owned_files:
      - dag-ml/docs/contracts/archive-v1/
      - dag-ml/scripts/validate_archive_v1_contract.py
      - dag-ml/tests/test_archive_v1_contract.py
      - dag-ml/.github/workflows/ci.yml
    acceptance_tests:
      - Archive V2 replay refs and producer_port contract tests passed
      - 24 frozen refusals each materialized as isolated ZIP and passed to validate_archive_zip
      - Real SQLite DB/WAL/SHM/rollback journal sidecars were generated in temporary storage and refused
      - Python 3.11 archive suite 11 passed against exact pinned legacy loader oracle
    review: Sol-xhigh ACCEPT. W1 pack mismatch and missing paired dag-ml-data branch make the global CI/oracle path unproven and are separate release blockers.
    rollback: revert only the archive contract/validator/tests/CI oracle block; do not modify historical archives, W1 packs, or user data.
  - id: SAVE-002
    owner: D + E
    dependencies:
      - SAVE-001
      - STORE-001
      - MTH-002
    state: minimal_methods_pls_archive_v2_run_save_close_load_predict_complete_generalization_pending
    input_sha: 7b762bc90e7b1b48377ca0f4cc5f2265e92f2b97
    owned_files:
      - nirs4all/nirs4all/api/native_archive_training.py
      - nirs4all/nirs4all/api/run.py
      - nirs4all/nirs4all/pipeline/engine.py
      - nirs4all/nirs4all/pipeline/dagml/raw_training_lowerer.py
      - nirs4all/tests/integration/api/test_native_archive_v2_training_cycle.py
    acceptance_tests:
      - explicit engine native lowers raw X/y/sample_ids, one splitter and one sklearn PLSRegression to controller:methods.pls without constructing PipelineRunner or installing an op callback
      - DAG-ML executes Methods training and exports Package V2 in portable_required/native_portable mode; Core alone writes and validates the Archive V2 container
      - a public run saves Archive V2/N4MM, closes all native handles, then a fresh process load_session predict and direct predict preserve requested sample IDs and finite identical values on new data
      - one-byte N4MM tampering is refused by the Core inventory hash/size validation before replay
      - default dag-ml and explicit legacy behavior remain unchanged; the additive native lane is fail-closed and has no implicit PipelineRunner fallback
      - selected Python RC passes 55 non-live tests with one opt-in live skip, Ruff and targeted Mypy; the exact live native gate passes all 56 tests
    review: >-
      Commit 7b762bc closes SAVE-002 only for the deliberately frozen portable
      minimum. The live witness used Core source
      fd89d75a19d86a398b382b521fd64693e504c636 and its 0.3.22 abi3 wheel
      (SHA-256 28838128b4bc7a615cc9c4d2ef1bfffb03a3874b9b06a758d6fe7e9638a99e6a),
      plus Methods source aabdecfdd76d1a4d12cfbbade4eeee0d30a6ea47 and
      libn4m.so SHA-256 95ae3b4df320ea232a180a0e5e251abfe8f9c9ab235a11384be8ed85bfecdccf.
      No wheel, shared library, archive or build output is tracked. This item
      did not by itself close general API-002/API-003: preprocessing, branches,
      HPO, conformal, multi-target and the default dag-ml producer path still
      required separate qualification. Subsequent API-002 evidence closes only
      the bounded Methods PLS multi-target lane.
    rollback: remove the additive engine native producer and retain the default dag-ml plus explicit legacy profiles; never rewrite or delete user archives.
  - id: API-003
    owner: E + C
    dependencies:
      - SAVE-002
    state: archive_v2_prediction_session_hardened_native_run_session_and_general_cache_pending
    input_sha: 6565c8ed885acc1a896c710fda693b4a540d7520
    owned_files:
      - nirs4all/nirs4all/api/session.py
      - nirs4all/nirs4all/api/__init__.py
      - nirs4all/nirs4all/__init__.py
      - nirs4all/nirs4all/pipeline/dagml/core_archive_replay.py
      - nirs4all/tests/unit/api/test_core_archive_session.py
      - nirs4all/tests/integration/api/test_native_archive_v2_training_cycle.py
    acceptance_tests:
      - a native Archive V2 Session performs multiple predictions while preserving requested sample IDs and never constructing PipelineRunner
      - the session cache retains only the validated Package V2 value plus the Core module and archive path; no Methods handle persists between predictions
      - Core revalidates the archive inventory on every replay, so an atomic archive replacement with a one-byte-tampered N4MM member is refused on the next prediction
      - cached Package V2 data is immutable from the caller and repeated predictions keep identical finite values
      - Session.close is terminal and idempotent, detaches each native result once, closes the runner and purges the archive cache
      - run, predict, retrain, save, runner access and context-manager re-entry all fail closed after close
      - selected Python RC passes 449 API tests and 477 DAG-ML/bundle tests with three declared skips; the full unit inventory passes 7863 tests with 148 declared skips outside four pre-existing binary sampler failures
      - Archive V2 run and retrain refuse as typed prediction-only capabilities before data or PipelineRunner access; legacy prediction is never an implicit fallback
      - the one-entry Session cache is capped at 8 MiB and bound to the full source archive SHA-256; archive hashing costs one bounded-memory read per prediction and Core still performs authoritative replay validation
      - SessionClosedError is the public top-level terminal error, close purges validation and fingerprint state, and a fresh load resumes finite multi-target prediction
      - independent focused gates pass 11 tests with one optional skip, and a fresh exact-wheel Core 0.3.22 plus Dag-ML 0.3.18 and libn4m live gate passes all 4 tests including multi-predict, close/reload, process replay and tamper refusal
    review: >-
      Python RC commits 1216eedf and 9b862952 close the Archive V2
      multi-predict and terminal Session lifecycle without broadening the
      portable minimum qualified by SAVE-002. Core remains the authority for
      archive integrity and revalidates bytes on every replay; the Python
      cache contains no native model handle and cannot bypass tamper checks.
      Canonical commit 6565c8ed adds the bounded source-fingerprint cache,
      public terminal/dependency errors and typed prediction-only run/retrain
      refusals. Although the agent environment lacked native wheels, an
      independent fresh-venv rerun used the exact selected Core, Dag-ML and
      Methods artifacts and passed 4/4 live tests.
      `run(..., session=..., engine="native")` remains explicitly refused
      before PipelineRunner construction. Native multi-run reuse and the
      general DAG/data cache are still API-003 work. At this checkpoint,
      preprocessing, branches, HPO, conformal and multi-target prediction
      remained API-002/001 work; the subsequent API-002 item closes only
      bounded Methods PLS multi-target prediction.
    rollback: remove only the additive native Archive V2 session cache/lifecycle path; preserve archives and the explicit legacy profile.
  - id: API-002
    owner: E + C
    dependencies:
      - SAVE-002
      - API-003
    state: native_methods_pls_archive_v2_multitarget_run_save_load_predict_qualified_general_dag_pending
    input_sha: 80eafd53ecf786429d8a9ceb7bae844beb6abcf8
    owned_files:
      - nirs4all/nirs4all/api/native_archive_training.py
      - nirs4all/nirs4all/pipeline/dagml/core_archive_replay.py
      - nirs4all/nirs4all/pipeline/dagml/raw_training_lowerer.py
      - nirs4all/tests/integration/api/test_native_archive_v2_training_cycle.py
      - nirs4all/tests/unit/api/test_core_archive_predict.py
      - nirs4all-core/bindings/rust/nirs4all/Cargo.toml
      - nirs4all-core/bindings/python/tests/test_archive_live.py
      - nirs4all-core/Cargo.lock
    acceptance_tests:
      - native raw training accepts y with shape N by Q; Q greater than one requires explicit non-empty unique target_names whose length exactly matches Q, while the qualified single-target surface remains unchanged
      - target_names cross the request, binding and Methods archive boundary and are returned in PredictResult metadata; any replay width mismatch fails closed
      - raw Dag JSON contracts remain authoritative and the multi-target path never constructs PipelineRunner, installs an operation callback or performs an implicit legacy fallback
      - a real N by 2 cycle trains, exports, closes, reopens a Session, predicts repeatedly and through a fresh process, and direct prediction preserves finite identical values and requested sample IDs
      - Core revalidates archive inventory and refuses one-byte N4MM tampering; the witness uses exact Dag-ML and dag-ml-core 0.3.18 plus the selected Methods ABI
      - Python Ruff and targeted Mypy pass, 21 targeted tests pass with one optional live skip, and the complete unit/API suite passes 414 tests
      - Core fmt, Clippy with warnings denied, 63 Rust tests and one doctest pass; two real archive live witnesses and the promoted Python N by 2 gate pass
    review: >-
      Python commit 80eafd53 promotes the bounded multi-target Archive V2 lane
      after Core commit 085ab611 pins both dag-ml and dag-ml-core to the
      selected 0.3.18 release. The qualified Core abi3 wheel SHA-256 is
      d08e4ff36b0b901105da8dc03f34064eec628e9d90786f34f95beea219d10e19,
      the Dag-ML wheel SHA-256 is
      db3394a4e2ea74b40038235e807e5f2b9027fc9eaf94557040e20216897c86dc,
      and the exact libn4m SHA-256 is
      95ae3b4df320ea232a180a0e5e251abfe8f9c9ab235a11384be8ed85bfecdccf.
      The generated archive witness SHA-256 is
      994252030ff80129d0431995bae53eb473082f05825b65714379262b72af13fa.
      This closes multi-target Methods PLS replay only. Preprocessing, branches,
      HPO, conformal, arbitrary controllers, native multi-run reuse and the
      default producer cutover remain open API-001/API-002/API-003 work.
      The Core wheel's historical omission of the additive n4a Python facade
      remains a packaging limitation and is not hidden by this evidence.
    rollback: remove only the multi-target lowering/replay additions and restore the prior Core Dag-ML pins together; preserve user archives and the explicit legacy profile.
  - id: API-004
    owner: E + B
    dependencies:
      - CAP-001
      - API-002
    state: bounded_dagml_full_bundle_retrain_qualified_transfer_finetune_core_and_plugin_pending
    input_sha: 79f4358b0ea4829cb18f28542dfce2af50973ed1
    owned_files:
      - nirs4all/nirs4all/api/retrain.py
      - nirs4all/nirs4all/api/retrain_capabilities.py
      - nirs4all/nirs4all/api/session.py
      - nirs4all/nirs4all/api/result.py
      - nirs4all/nirs4all/pipeline/bundle/loader.py
      - nirs4all/tests/unit/api/test_retrain.py
      - nirs4all/tests/unit/api/test_core_archive_session.py
      - nirs4all/tests/integration/api/test_dagml_native_retrain_roundtrip.py
      - nirs4all/tests/integration/parity/test_parity_smoke.py
    acceptance_tests:
      - default full retrain accepts only a concrete n4a bundle with one bounded train_pipeline.json contract, probes the real DAG-ML backend first and executes a new native run without constructing PipelineRunner
      - the native full lane produces a distinct refit artifact and finite predictions; source bundle and training-spec digests, mode, engine and artifact count form additive retrain lineage
      - exported retrained n4a provenance persists that lineage, BundleLoader validates and exposes it, and the exported artifact remains predict-capable
      - transfer, finetune, Core-native, unavailable plugin, fallback-enabled selection and unsupported options refuse before source/data materialization, legacy loaders, computation or writes
      - PipelineRunner is reachable only through explicit engine legacy selection during the ADR-24 rollback window; public signatures remain frozen and preflight discovery is additive
      - independent qualification passes Ruff, focused Mypy and 27 retrain/Core/integration/parity tests; the final full inventory passes 7861 tests with 148 declared skips and four pre-existing binary sampler exclusions
    review: >-
      Candidate 7dd25ad was independently audited and promoted as Python
      commit 79f4358b. This closes only the concrete DAG-ML full retrain path
      backed by an exported training specification. Transfer, finetune,
      Core Archive V3 and plugin adapters remain typed refusals. The immutable
      native-results manifest is written by run before retrain attaches source
      lineage and is deliberately not backpatched; durable lineage is carried
      by the existing public n4a export provenance contract instead. General
      retrain, HPO-controller integration and LOCK-API therefore remain open.
    rollback: remove the additive retrain capability router and provenance field, restoring only the explicit legacy retrain path without modifying user bundles or native result directories.
  - id: API-005
    owner: E
    dependencies:
      - CAP-001
    state: advanced_verb_preflight_complete_native_and_plugin_execution_refused_pending_callable_contracts
    input_sha: 48a85680c6c49fd6a293dcb45d049a3debe0c884
    owned_files:
      - nirs4all/nirs4all/api/advanced_capabilities.py
      - nirs4all/nirs4all/api/explain.py
      - nirs4all/nirs4all/api/generate.py
      - nirs4all/tests/unit/api/test_explain.py
      - nirs4all/tests/unit/api/test_generate.py
      - nirs4all/tests/integration/parity/test_parity_smoke.py
      - nirs4all/tests/regression/test_public_api_contract.py
    acceptance_tests:
      - explain and every generate namespace surface perform a side-effect-free capability decision before Session, PipelineRunner, synthesis or loader imports, data normalization, path access, compute or output writes
      - default dag-ml/native selection returns a typed unsupported-capability RtError because no callable Core or DAG-ML explain/generate contract exists
      - explicit plugin selection is also refused honestly until a callable V1 adapter is installed; allow_fallback true is never accepted
      - the historical Python SHAP and synthesis implementations remain reachable only through explicit engine legacy selection or the documented N4A_ENGINE legacy profile for frozen helper signatures
      - public 0.9.x signatures remain unchanged and preflight discovery is additive through explain.preflight and generate.preflight
      - independent qualification passes Ruff, targeted Mypy, 489 API and public-contract tests with one opt-in live skip, plus two explicit-legacy parity explain tests
    review: >-
      Candidate commit 0821c82 was independently rerun and promoted as Python
      commit 48a85680. API-005 closes executable routing and early refusal, not
      scientific implementation: the capability ledger deliberately records
      native and plugin execution as false. The full unit inventory passed
      7,844 tests with 148 declared skips outside four pre-existing binary
      sampler failures unrelated to this item. A future callable Core/DAG or
      bounded plugin contract must add new differential evidence before either
      capability can become true.
    rollback: remove only the additive advanced-capability preflight and restore the explicit legacy calls in affected tests; retain user data and rollback distributions.
  - id: SAVE-003
    owner: D
    dependencies:
      - SAVE-001
      - STORE-001
    state: duckdb_multirun_n4a_mixed_artifact_conversion_qualified
    input_sha: d6ddcd3a4f012ab1641ab350ec67eb1dacb87272
    owned_files:
      - nirs4all-tools/src/nirs4all_tools/duckdb_workspace.py
      - nirs4all-tools/src/nirs4all_tools/n4a_archive.py
      - nirs4all-tools/src/nirs4all_tools/n4mm_export.py
      - nirs4all-tools/src/nirs4all_tools/trusted_joblib.py
      - nirs4all-tools/src/nirs4all_tools/commands.py
      - nirs4all-tools/tests/e2e/test_legacy_save_predictions_web.py
      - nirs4all-tools/tests/test_duckdb_workspace.py
      - nirs4all-tools/tests/test_n4a_archive.py
      - nirs4all-tools/tests/test_n4mm_export.py
    acceptance_tests:
      - the selected Tools lineage is linear from origin main b89da99 through DuckDB workspace aa3750c, multi-run 081c7de and transactional preflight 8ce93ed to d6ddcd3
      - announced DuckDB and multi-run fixtures are semantically lowered transactionally or refused before output publication
      - trusted sklearn PLS joblib exports a public portable N4MM with CLI code 0; safe best-effort n4a preservation returns code 10 and later verifies with code 0; strict unsupported conversion returns code 20 without output
      - an external Rust consumer loads the exported N4MM through the public Core 0.3.22 API and predicts finite values identical to sklearn; one-byte tampering is refused
      - the selected Tools RC passes 319 tests with no skip, Ruff, Mypy over 19 files, and builds a pure-Python sdist/wheel without native or model artifacts
    review: >-
      The real portable witness uses Methods 1.0.13 ABI 2.3.0 and produces
      model.n4mm SHA-256 b7ce813d19565a03d949a9f22d901eb57438585ca2183eba1a2cfe8e7b8c60a3.
      Core fd89d75 / crate 0.3.22 loads and predicts values
      [7.68181818181818254, -3.54545454545454586], matching sklearn. This
      proves the public PREDICT-only N4MM conversion boundary, not production
      of a complete Core Archive V2; SAVE-002 owns that container lifecycle.
      The historical Python rerun witness now selects engine legacy explicitly
      under ADR-24, preserving rollback without relying on a default-engine
      fallback.
    rollback: remove only the additive DuckDB/archive/N4MM conversion paths; never alter or delete source workspaces, archives or models.
  - id: SAVE-004
    owner: D + G
    dependencies:
      - SAVE-003
    state: transactional_conversion_security_and_source_immutability_qualified
    input_sha: d6ddcd3a4f012ab1641ab350ec67eb1dacb87272
    owned_files:
      - nirs4all-tools/src/nirs4all_tools/policy.py
      - nirs4all-tools/src/nirs4all_tools/commands.py
      - nirs4all-tools/src/nirs4all_tools/n4a_archive.py
      - nirs4all-tools/tests/test_policy.py
      - nirs4all-tools/tests/test_commands.py
      - nirs4all-tools/tests/test_n4a_archive.py
    acceptance_tests:
      - focused transactional and security gate passes 23 tests including ENOSPC atomicity for absent and existing-empty targets
      - dry-run returns code 0 without output and preserves source bytes; opaque preservation copies remain byte-identical
      - inventory/checksum mismatch, ZIP traversal and seven compression/zip-bomb limits return code 20 without output
      - trusted joblib extraction is explicit and bounded; unsupported strict conversion never reports a false code 0
      - source SHA-256 is unchanged before and after every exercised conversion path
    review: >-
      Qualification ran both focused adversarial probes and the complete 319-test
      suite against the selected Python RC and Dag-ML runtime. The selected
      branch is clean and commits no wheel, shared library, n4a, n4mm, joblib or
      build output. Resumable conversion claims remain limited to the published
      transaction protocol; no in-place migration of user sources is allowed.
    rollback: disable conversion publication and retain read-only detection/reporting; source inputs remain immutable and recoverable.
  - id: STORE-001
    owner: D + E
    dependencies:
      - SAVE-001
      - MTH-002
    state: complete
    input_sha: 1dd67bed2b12f2290103640c838e96274f0a39d0
    owned_files:
      - nirs4all-core/bindings/rust/nirs4all/src/archive_v1.rs
      - nirs4all-core/bindings/rust/nirs4all/Cargo.toml
      - nirs4all-core/Cargo.lock
      - nirs4all-core/bindings/rust/nirs4all/tests/parity/
      - nirs4all-core/bindings/rust/nirs4all/LICENSES/
      - nirs4all/nirs4all/api/predict.py
      - nirs4all/nirs4all/api/session.py
      - nirs4all/nirs4all/pipeline/dagml/core_archive_replay.py
      - nirs4all/tests/unit/api/test_core_archive_predict.py
      - nirs4all/tests/unit/api/test_core_archive_session.py
    acceptance_tests:
      - public reader/writer strictly validate stored archive V1 ZIP/manifest/inventory/reference/workspace rules before payload materialization and never replace an existing output
      - two normative positives and all 24 frozen refusals are materialized through the public reader from a local hash-provenance mirror
      - source and extracted packages both passed 31 Rust tests; archive dag-ml validator/unittests and full validate_contracts passed
      - Python binding exposes strict Methods Archive V2/V3 replay through py.allow_threads; Rust passed 64 tests and Python passed 91 with one skip
      - real 0.3.22 abi3 wheel imported both entrypoints (SHA-256 b7a1de538208ec61f8db94977239410e0b5e71c1fccf21f26f335fc1edbbe90e); a real pre-bridge wheel and missing extension fail closed
      - real 0.3.22 abi3 wheel replayed a two-feature Archive V2 through Methods aabdecf and libn4m, preserving predict.0/predict.1 and finite values; one-byte N4MM tampering was refused on inventory hash/size mismatch (Core fd89d75)
      - nirs4all predict detects exact Core V2 manifests before any PipelineRunner construction and directly invokes replay_methods_archive_v2; V3, explicit legacy, old/missing Core and invalid outputs fail closed while non-Core inputs retain the historical path (8 focused API tests)
      - load_session detects and validates Core V2 before BundleLoader, returns a native session whose prediction never constructs PipelineRunner, and refuses V2 retrain plus V3 prediction semantics; the frozen predict/load_session signatures and 41 API contract tests pass
    review: >-
      Sol-xhigh ACCEPT for the strict stored-ZIP native archive reader/writer. Core
      commits 8ac95bbb and 39b71b47 add the Methods-only V2/V3 replay bridge and
      real-wheel compatibility refusal evidence. Follow-up Core commit fd89d75
      adds an opt-in release witness and proves a real wheel plus local libn4m
      replays an integrity-valid Archive V2 with aligned, finite predictions;
      an altered N4MM member refuses before replay. This closes the positive
      Core bridge witness, while serialized_model_predict remains false:
      nirs4all commit 0e920ecf adds the strict V2 public predict adapter without
      constructing PipelineRunner and refuses V3 as full-refit/retrain rather
      than mislabelling it serialized prediction. Commits d304ae1d and 1dd67bed
      extend the same fail-closed bridge to load_session/Session while preserving
      both frozen public signatures. SAVE-002 run-save-load-predict remains
      open because this witness starts from an already valid Archive V2; it
      does not prove that the public training/save path produced that archive.
    rollback: remove only Core archive/store package additions and mirrored synthetic contract corpus; retain dag-ml contract, legacy source archives and user data unchanged.
  - id: STORE-002
    owner: D + F
    dependencies:
      - STORE-001
      - STU-001
    state: native_store_reads_and_run_detail_owner_cpython_boundary_qualified_on_python_rc
    input_sha: 030a9f94bba2112d2d72102c9767533b62bee1a3
    owned_files:
      - nirs4all/nirs4all/pipeline/storage/contracts/workspace_store_read_v1.json
      - nirs4all/nirs4all/pipeline/storage/contracts/workspace_store_results_summary_v1.json
      - nirs4all/nirs4all/pipeline/storage/contracts/studio_run_detail_http_v1.json
      - nirs4all/nirs4all/pipeline/storage/workspace_read_contract.py
      - nirs4all/nirs4all/pipeline/storage/workspace_store.py
      - nirs4all/nirs4all/pipeline/storage/__init__.py
      - nirs4all/pyproject.toml
      - nirs4all/tests/regression/test_workspace_store_read_contract.py
      - nirs4all/tests/unit/pipeline/storage/test_workspace_store.py
      - nirs4all-studio/sidecar/contracts/workspace_store_read_v1.json
      - nirs4all-studio/sidecar/src/workspace_store.rs
      - nirs4all-studio/sidecar/tests/fixtures/workspace_store_v5.sqlite
      - nirs4all-studio/sidecar/tests/fixtures/workspace_store_v5_runs.response.json
      - nirs4all-studio/sidecar/tests/fixtures/workspace_store_v5_results.response.json
    acceptance_tests:
      - WorkspaceStore v5 run-summary projection opens store.sqlite with SQLite immutable read-only mode and executes against a fresh Store without creating WAL or SHM files
      - contract resource is packaged under nirs4all.pipeline.storage and rejects a contract whose declared schema version differs from the installed WorkspaceStore schema
      - focused pytest and Ruff pass; the module passes Mypy with dependency following disabled while the project-wide configured Mypy baseline retains unrelated failures
      - native sidecar contract copy is byte-identical to the published resource (SHA-256 7147b9ad47b39d4d36ab39a72208ca888dd5693b1b3c1fcd72d45bc7cafde9c5)
      - Store v5 run-summary timestamps declare ISO 8601 serialization; the native reader normalizes the SQLite separator and its complete response equals the Python-written fixture
      - native reader accepts only exact Store v5 run-summary fields, refuses active SQLite journals and database changes, and reads the Python-written fixture without WAL or SHM creation
      - public Store v5 pipeline-summary projection declares its count query, nullable fields and deterministic public list_pipelines ordering; the Python-written SQLite fixture is SHA-256 6d993625d44196a2812b3497b9776ada007ce099c88e904dba951ffa17460491
      - public studio_chain_ranked_v1 projection requires explicit dataset, metric and asc/desc direction, bounds pages to 100, uses a matching count query, sorts null-last with chain_id tie-breaking, and excludes chains without predictions
      - producer regression tests prove asc/desc ordering, stable offset/count, mixed-metric filtering, pending-pipeline inclusion and prediction eligibility against Store v5 without publishing the private Studio results-summary policy
      - separate results-summary policy v1 (SHA-256 e497e7db0a5043626e5e1a64ecb14bce5523ac12c06193c982b8f2eef8ff7d70) preserves read-v1 bytes while publishing the full paged source projection, fixed bare n=5 surface, chain_id source order, one metric-direction registry, refit synthesis, selection, JSON/null serialization and variant-param merge
      - query_top_chains now applies chain_id ASC as a stable score-tie breaker; unknown metrics use the results-summary policy default higher-is-better at every summary ranking stage
      - studio_run_discovery_query_v1 publishes the exact source unified/manifests/parquet and refresh true/false scanner query allowlist with immutable Store-v5 read semantics, duplicate/unknown rejection and no fallback (SHA-256 354ec768f883ec6f4a1fa3831cf650f030920b08792dbe4e92c757478e129637)
      - studio_run_detail_v1 publishes the deterministic Store-owned run/pipeline/log projection with recursive non-finite normalization, immutable read and fail-closed schema/journal/change/JSON checks (contract SHA-256 91733eab5de6ace6f3bb3e6534c46daf4055dcc343f26da0d85b7c8b30c82e27)
      - studio_run_detail_http_v1 publishes exact Store/splitter/results inputs and ownership boundaries, then owner commit b4b04b8f materializes splitters plus six optional runtime columns with explicit stored/absent provenance; the qualified current contract SHA-256 is 8230963eeb317ccacf5fa83a29fec730a830ebbb81ead9d16629251a1993ab1 and golden SHA-256 is 1053274a5d5a900bb3511afc3290c0adae5a1c2b84beacb742fd650f806c19bd
      - historical Store-v5 databases without runtime columns emit null plus absent_in_store_v5; present text/JSON columns are shape-checked, non-finite values normalize recursively, and malformed data refuses under immutable before/after database stamps
      - public owner callable studio_run_detail_http_inputs_v1 accepts only workspace_path and run_id, derives splitter data from historical expanded_config before the consumer boundary, and exposes no native ABI or Store-v5 schema change
      - a fresh isolated CPython subprocess with explicit host dependency roots returns strict finite JSON without FastAPI, without constructing PipelineRunner, without SQLite byte changes or journal sidecars, and with a 30-second test bound
    review: >-
      Phase 2 evidence: nirs4all commits 56a9eb1e, ece5d7fd, 0288b1e5,
      09702901 and b7a5ff3e extend the public v5 contract
      with a bounded pipeline-summary projection, count query, nullable values and
      deterministic list_pipelines ordering, followed by an explicitly directed,
      deterministic studio_chain_ranked_v1 primitive. Studio commits d1260b1 and
      73b3aec consume its byte-identical copy through strict Rust readers. The readers check PRAGMA
      user_version == 5, projection columns, bound limit/offset, active journal
      sidecars, and snapshot stability before returning Python-written run and
      pipeline fixtures. The Electron transport selects only bare run-summary and
      filter-free pipeline-summary GET requests in opt-in sidecar mode. Owner
      commit 24c06c22 additionally publishes the strict source/refresh run-discovery
      query policy. Scan POST, filtered results, enriched runs, run detail, predictions,
      results-repository, mutation, arrays, artifacts, and scientific execution remain
      FastAPI-owned; native query or compatibility rejection never triggers a hidden
      FastAPI retry. Local-volume SQLite is the supported surface; Windows UNC/device
      paths remain refused pending packaging and real-share evidence. This is not a
      scanner/repository cutover and does not satisfy R2 or any R3/R4 drop gate.
      Owner commit 55688872 adds only the Store-owned run-detail projection;
      dataset mapping, CV/runtime derivation, rerun readiness, results and legacy
      manifest composition remain explicitly outside this slice.
      Owner commit cb12f46f adds exact splitter and result inputs, but correctly
      assigns linked-dataset mapping, CV/runtime/rerun policy and legacy manifest
      discovery to their actual owners instead of moving them into Store code.
      Owner commit b4b04b8f publishes splitter materialization before the consumer
      boundary and a separate ordered runtime projection for engine,
      engine_requested, engine_diagnostics, runtime_manifest, fallback_policy
      and native_result_refs without changing the historical read-v1 contract.
      Studio still owns composition vocabulary, dataset mapping and rerun policy;
      the legacy-manifest branch or an exact per-request preselection proof remains
      mandatory before route selection.
      The complete storage chain from f2583062 through b4b04b8f is promoted
      onto the selected Python RC as commits 24f39352 through b0d97cea. The
      contracts, policy module and goldens are byte-identical to the owner
      lineage; the RC retains its independent Windows atomic-replace fix and
      API/session work. On that selected RC, 20 storage/splitter tests, 38
      public API/archive-session contract tests, Ruff and targeted Mypy pass.
      Follow-up Python RC commits f3d83a98 and 030a9f94 make the already
      exported `studio_run_detail_http_inputs_v1(workspace_path, run_id)`
      boundary explicit and qualify it through a fresh `python -I` process.
      Contract SHA-256 is
      8230963eeb317ccacf5fa83a29fec730a830ebbb81ead9d16629251a1993ab1;
      the owner golden remains
      1053274a5d5a900bb3511afc3290c0adae5a1c2b84beacb742fd650f806c19bd.
      The isolated host receives only explicit site/dist-packages roots, not
      PYTHONPATH or sibling sources, and failures preserve child stdout/stderr.
      This closes the Python owner materializer boundary, not the Studio route:
      bounded Rust host invocation and response composition remain STU-005.
    rollback: remove only the published contract/resource/test; do not alter existing workspace databases or conversion outputs.
  - id: STU-005
    owner: D + F
    dependencies:
      - STU-001
      - STORE-002
    state: native_store_run_detail_owner_host_route_qualified_scientific_execution_still_unselected
    input_sha: 23d1596a7b5139b1c8c442d13a8430768539961d
    owned_files:
      - nirs4all-studio/sidecar/Cargo.toml
      - nirs4all-studio/sidecar/Cargo.lock
      - nirs4all-studio/sidecar/README.md
      - nirs4all-studio/sidecar/contracts/workspace_store_read_v1.json
      - nirs4all-studio/sidecar/contracts/workspace_store_results_summary_v1.json
      - nirs4all-studio/sidecar/contracts/studio_run_detail_preselection_v1.json
      - nirs4all-studio/sidecar/contracts/studio_run_detail_http_v1.json
      - nirs4all-studio/sidecar/contracts/studio_run_detail_composition_v1.json
      - nirs4all-studio/sidecar/src/lib.rs
      - nirs4all-studio/sidecar/src/results_summary.rs
      - nirs4all-studio/sidecar/src/run_detail.rs
      - nirs4all-studio/sidecar/src/run_detail_cpython.rs
      - nirs4all-studio/sidecar/src/run_detail_preselection.rs
      - nirs4all-studio/sidecar/src/settings.rs
      - nirs4all-studio/sidecar/src/workspace_store.rs
      - nirs4all-studio/sidecar/tests/fixtures/workspace_store_v5.sqlite
      - nirs4all-studio/sidecar/tests/fixtures/workspace_store_v5_runs.response.json
      - nirs4all-studio/sidecar/tests/fixtures/workspace_store_v5_results.response.json
      - nirs4all-studio/sidecar/tests/fixtures/workspace_store_v5_summary.sqlite
      - nirs4all-studio/sidecar/tests/fixtures/workspace_store_v5_summary.response.json
      - nirs4all-studio/sidecar/tests/fixtures/workspace_store_v5_summary_dataset_links.json
      - nirs4all-studio/sidecar/tests/run_detail_owner_differential.rs
      - nirs4all-studio/sidecar/tests/run_detail_preselection_contract.rs
      - nirs4all-studio/src/api/transport.ts
      - nirs4all-studio/src/api/transport.test.ts
      - nirs4all-studio/src/api/linkedWorkspaces.ts
      - nirs4all-studio/src/types/runs.ts
      - nirs4all-studio/electron/workspace-route-preselection.ts
    acceptance_tests:
      - direct native GET /api/workspaces/:id/runs returns the complete Python-written Store v5 run-summary fixture without a configured Python plugin host
      - Electron selects bare and strict allowlisted source/refresh run-summary queries; enriched/detail routes and mutations remain FastAPI-owned, and a native 409 compatibility refusal never retries FastAPI
      - the native reader is immutable and fail-closed for live SQLite journals, non-v5 schemas, incomplete projections, absent stores, and out-of-range requests
      - direct native bare GET /api/workspaces/:id/results returns the exact Python-written Store v5 pipeline-summary fixture, including nullable values, total count and has_more; query-bearing results requests are rejected before native routing
      - Electron selects only the filter-free pipeline-summary endpoint; query-bearing results, chain ranking, predictions and every mutation remain FastAPI-owned, and a native refusal never retries FastAPI
      - focused Python summary oracle and Store integration pytest passed (4); Ruff and Mypy passed on their previously recorded focused surfaces
      - Studio Vitest passed 519 files / 3,739 tests; root/app/node TypeScript checks and targeted transport ESLint passed; Rust format/clippy passed with 51 unit and 5 frozen sidecar contract tests
      - public Rust workspace_store reader consumes studio_chain_ranked_v1 without metric inference and passes asc/desc, tie, null-last, offset/count, mixed-metric, invalid-JSON, eligibility and bound tests; full Rust gate passes 42 unit and 5 frozen contract tests
      - native dataset-links reader projects only id/name/path, rejects files larger than 2 MiB, preserves missing/malformed legacy empty-catalogue semantics, and never rewrites or migrates scientific dataset configuration; Rust gate passes 45 unit and 5 frozen contract tests
      - Python-generated Store v5 summary fixture (SHA-256 26db7b8dcfefa815f9581ef0f4934ebda5f0a658819e6e2c2b41605ffb3d75d7) and golden (SHA-256 f2083990efc070e09b7eaa34613f32e8c0316364fc7d17d6fd41de08bb5a0b58) cover 24 pipelines across r2, rmse and unknown metrics, ties/nulls, refit-only, synthetic refit, best-final outside top five, variant params and dataset-link matching
      - summary oracle now passes the owner metric direction explicitly to query_top_chains; Store score ties are deterministic by chain_id, and generated/replayed golden parity tests pass
      - immutable reader canonicalizes store symlinks before journal checks, refuses a canonical target with an active WAL, and declares local-filesystem-only plus Windows UNC/device exclusions in the byte-identical public contract
      - direct native bare GET /api/workspaces/:id/results/summary consumes the byte-identical public policy, pages all source rows by chain_id within one immutable snapshot, and equals the Python golden exactly; a 501-row regression crosses the fixed 500-row page boundary
      - the summary route rejects query-bearing requests before routing, returns 405 for mutations, fails closed for missing/non-v5/busy stores, exposes no configurable n surface, and never retries or falls back to Python/FastAPI after native selection
      - refit-only append marking matches the Python oracle for mixed CV/final payloads, while persisted Python NaN and infinities are recursively normalized to null without discarding finite JSON siblings
      - direct native run discovery accepts only source unified/manifests/parquet and refresh true/false, matches seven exact Python goldens, rejects duplicate/unknown values before selection, and returns Store-v5 compatibility errors without FastAPI retry
      - Rust consumes studio_run_detail_v1 and the owner-produced studio_run_detail_http_inputs_v1 envelope without reconstructing splitters or parsing expanded_config
      - Studio consumes owner HTTP contract SHA-256 8230963eeb317ccacf5fa83a29fec730a830ebbb81ead9d16629251a1993ab1 and owner golden SHA-256 1053274a5d5a900bb3511afc3290c0adae5a1c2b84beacb742fd650f806c19bd
      - studio_run_detail_composition_v1 freezes splitter vocabulary, runtime/provenance propagation, linked-dataset mapping, results alignment and rerun_ready; its Store-v5 response golden matches the FastAPI oracle without parsing expanded_config
      - integrated sidecar gate passes 82 Rust tests plus fmt/clippy; native composition and Store adapter pass 5 Python tests, RunDetail UI passes 5 Vitest tests, and TypeScript/Ruff/ESLint pass
      - studio_run_detail_preselection_v1 SHA-256 42b076b3cc31c167ac4366eaabb394876d84c40a044991217e2f2823a57b92c4 resolves linked workspace ID and classifies exact Store v5, legacy/absent, busy and incomplete stores before every target request without caching
      - exact Store v5 selects the native route only after the configured CPython library host preflights the exact owner callable; legacy/absent selects the transitional plugin and busy/incomplete rejects before a target request
      - each preflight and owner materialization uses fresh CPython isolated mode, strict JSON stdin, a 15 second deadline, 8 KiB input, 4 MiB stdout and 64 KiB stderr; malformed, oversized, timed-out or non-seven-field output fails closed
      - Rust revalidates Store v5 immutably on the target request, validates the owner envelope, composes the response and exposes neither stderr nor the resolved workspace path; no fallback follows native selection
      - combined selected Studio RC passes 110 Rust tests with one opt-in differential ignored, fmt and Clippy warnings denied; the real fresh-CPython owner differential passes 1/1, focused Vitest passes 34, TypeScript and targeted ESLint pass
    review: >-
      Studio commit d1260b1 selects bounded direct sidecar readers only for bare Store
      v5 run-summary and filter-free pipeline-summary GET requests, verifying exact
      Python fixture responses, nulls, deterministic score order, total count, query
      rejection and fail-closed compatibility refusals. This opt-in routing does not
      reclassify source/refresh scanner queries, filtered or parameterized results
      summaries, chain ranking, legacy manifest/Parquet discovery, enriched runs, run
      detail, predictions, results-repository, mutations, arrays, artifacts, or
      scientific execution: FastAPI remains their product owner. Studio commit 73b3aec
      additionally consumes the owner-published ranked-chain primitive through the
      public Rust workspace_store module, with explicit direction and deterministic
      paging. The owner policy is published by nirs4all 0288b1e5 and b7a5ff3e;
      Studio commits 2a3833f and e6b2498 freeze its deterministic Python oracle and
      transport selection. Studio e9b9a72 consumes the byte-identical policy in Rust,
      reads every source page from one immutable Store-v5 snapshot, and serves only
      the bare GET summary surface with exact golden parity and fail-closed errors.
      Studio f70e42e closes independently reviewed refit-marker and non-finite JSON
      edge cases without adding a retry or hidden FastAPI fallback after native selection.
      Studio 05ed4e7 consumes owner commit 24c06c22's byte-identical discovery
      policy and routes the strict source/refresh allowlist natively; scan POST,
      enriched/detail and repository surfaces remain FastAPI-owned.
      Studio cdf0c9f consumes the byte-identical owner run-detail projection as
      a strict immutable Rust reader without selecting the HTTP route. This is
      an honest prerequisite only: the missing response-composition contracts
      block cutover, so transport still selects FastAPI and no retry boundary is
      weakened.
      Studio 5d00f62 consumes the HTTP-input contract without activating a route:
      owner splitter output and runtime columns are still absent from the Store
      projection, dataset/runtime policies are unpublished, and Electron lacks a
      per-workspace Store-v5 preselection proof. Changing the contract's
      route_selection from forbidden fails closed.
      Studio commit e7189d0 adds the bounded,
      read-only dataset-link identity projection required for future mapping without
      exposing or mutating scientific loader configuration. The selected readers
      have no implicit Python or FastAPI fallback. Commit fa9fb0f proves the
      Store response composition, and e88d83e proves exact per-request
      preselection before target HTTP: legacy/absent stores select the explicit
      transition plugin, while busy/incomplete stores reject. Exact Store v5
      also rejected because its SQLite projection did not persist the splitter
      and result inputs required by studio_run_detail_http_inputs_v1. At that
      checkpoint, the route was not registered and Rust remained forbidden to
      parse expanded_config. Python RC f3d83a98
      and 030a9f94 subsequently publish and qualify that owner boundary.
      Studio commits 4cd4864 and 5d13d1d invoke it through a bounded fresh
      CPython library host, revalidate Store v5 in Rust, compose the response
      in Rust, and activate only the exact preselected target with no fallback.
      Commit 23d1596 keeps the run-detail and native-job capability assertions
      coherent after integration. This is not Python HTTP execution and does
      not enable scientific job submission. Scanner and repository surfaces
      without public native contracts remain out of scope; LOCK-STUDIO, R2
      parity, and every R3/R4 drop claim remain pending.
    rollback: remove only the sidecar reader, copied public contract, fixture, and dependency additions; leave linked-workspace metadata and user workspace files unchanged.
  - id: STU-004
    owner: A + F
    dependencies:
      - STU-002
      - STU-005
    state: native_submission_cpython_preflight_and_rust_only_renderer_selection_qualified_scientific_executor_pending
    input_sha: cdb9f46de1643ab170934481cd9471e54ed3701c
    owned_files:
      - nirs4all-studio/sidecar/contracts/studio_job_lifecycle_v1.json
      - nirs4all-studio/sidecar/contracts/studio_execution_job_record_v1.json
      - nirs4all-studio/sidecar/contracts/studio_scientific_submission_v1.json
      - nirs4all-studio/sidecar/contracts/studio_renderer_transport_selection_v1.json
      - nirs4all-studio/sidecar/contracts/studio_scientific_cpython_host_v1.json
      - nirs4all-studio/sidecar/src/execution_job_records.rs
      - nirs4all-studio/sidecar/src/job_http.rs
      - nirs4all-studio/sidecar/src/job_lifecycle.rs
      - nirs4all-studio/sidecar/src/lib.rs
      - nirs4all-studio/sidecar/src/scientific_submission.rs
      - nirs4all-studio/sidecar/src/scientific_cpython.rs
      - nirs4all-studio/sidecar/src/websocket_transport.rs
      - nirs4all-studio/sidecar/tests/scientific_submission_runtime.rs
      - nirs4all-studio/sidecar/tests/scientific_cpython_runtime.rs
      - nirs4all-studio/electron/main.ts
      - nirs4all-studio/electron/preload.ts
      - nirs4all-studio/electron/python-http-diagnostic-policy.ts
      - nirs4all-studio/electron/renderer-transport-selection.ts
      - nirs4all-studio/electron/renderer-transport-selection.test.ts
      - nirs4all-studio/src/api/transport.ts
      - nirs4all-studio/src/api/transport.test.ts
      - nirs4all-studio/src/api/renderer-transport-coverage.test.ts
      - nirs4all-studio/src/components/spectra-synthesis/ExportDialog.tsx
      - nirs4all-studio/src/lib/websocket.ts
      - nirs4all-studio/src/lib/websocket.test.ts
      - nirs4all-studio/src/types/electron.d.ts
      - nirs4all-studio/sidecar/tests/job_http_runtime.rs
      - nirs4all-studio/sidecar/tests/job_lifecycle_contract.rs
      - nirs4all-studio/sidecar/tests/job_lifecycle_state.rs
      - nirs4all-studio/sidecar/README.md
    acceptance_tests:
      - contract matches the frozen Studio-v1 five statuses, eleven job types and five cancellation endpoints
      - contract matches all three frozen WebSocket endpoints, emitted job-event envelope keys and declared-unreachable job_cancelled behavior
      - targeted Rust contract tests passed 2/2 and Clippy passed with warnings denied
      - bounded native registry implements strict transitions, terminal-only TTL/eviction, cooperative cancellation, finite clamped progress and exact legacy public fields/events
      - legacy metrics update is shallow-merge while progress preserves the stored snapshot; cancellation emits job_failed and no job_cancelled variant exists
      - integrated sidecar all-target gate passed 82 tests with fmt and Clippy warnings denied
      - real loopback RFC 6455 upgrades serve /ws, /ws/job/{job_id} and /ws/training/{job_id}; job aliases auto-subscribe, JSON ping emits pong, dynamic subscribe/unsubscribe retain the frozen refusal, and renderer envelopes never expose internal protocol_version/sequence
      - each connection queue is bounded to 64 messages and saturated/dead peers are pruned; live handshake, exact channel delivery, unreachable job_cancelled, malformed envelopes and backpressure are tested
      - NativeJobRuntime shares one JobRegistry and WebSocketConnectionManager; three frozen status GET aliases and all five cancellation POST aliases route to that authority
      - pending cancellation becomes terminal and emits exactly one legacy job_failed event; running cancellation remains cooperative and emits nothing until acknowledged; job_cancelled remains unreachable
      - response envelopes expose exactly the four renderer keys and enforce the event-size budget before state mutation; the shared WebSocket queue remains bounded to 64
      - real loopback HTTP plus RFC 6455 proves cancellation reaches the shared registry and publishes the exact renderer failure without internal fields
      - both frozen durable execution-job-record GET routes require an active linked workspace and read bounded per-run snapshots without consulting the in-memory registry or invoking Python
      - the durable reader rejects symlinks, workspace escape, oversized or changing files, malformed or unknown fields and payload identity mismatch; legacy missing progress fields normalize explicitly
      - known run identities are joined through the immutable Store v5 projection while orphan job lookup remains bounded to request.run_name; neither route retries FastAPI after native selection
      - the closed scientific submission contract rejects oversized, unknown, legacy, fallback and skipped fields before workspace resolution, executor preflight, registry mutation or durable writes
      - Rust owns submission validation, job registration, start event, durable record, cancellation and transport; the default executor remains unselected and returns 503 with scientific_execution=false
      - Electron preflights live sidecar capabilities before each native HTTP request or WebSocket connection and carries the selected URL in the decision; malformed or mismatched candidates reject without Python acquisition, retry or fallback
      - native renderer HTTP includes submission, status, cancellation and durable reads; only auto-subscribed /ws/job/{id} and /ws/training/{id} are native, while dynamic /ws remains selected to the plugin before connection because its subscribe protocol is not native-compatible
      - combined selected Studio RC passes 126 Rust tests with one opt-in owner differential ignored, fmt and Clippy warnings denied; the real fresh isolated CPython owner differential passes 1/1 after integration
      - the complete frontend passes 544 files and 3851 Vitest tests, TypeScript, targeted ESLint and the Electron development build
      - the explicit cpython-stdio-v1 acquisition uses a fresh isolated CPython 3.11+ process, a closed JSON response, a three-second deadline and bounded stdout/stderr; interpreter bytes are identity-bound and acquisition is sticky
      - CPython host absence, tamper, timeout, malformed or oversized output, wrong implementation/version/isolation and failed socket.bind audit guard return a typed 503 before body parsing, workspace access, registry mutation, events or durable writes
      - no current Python callable satisfies the closed Studio submission while preserving Rust ownership, so scientific_execution remains false and even a preflight-ready impostor cannot accept a job
      - the selected increment passes 133 Rust tests with one opt-in differential ignored, real CPython 3.11 preflight and bind refusal, 3861 Vitest tests, TypeScript, Electron build and ESLint with zero errors
      - packaged and default development sessions freeze a Rust-only policy before requests; non-native HTTP and WebSocket paths return typed 501 before fetch, URL acquisition, runtime preparation or Python spawn
      - only the visible --enable-python-http-diagnostic switch can assign a packaged renderer session to FastAPI; the exact environment opt-in is development-only and ignored in packages
      - diagnostic mode selects the Python HTTP owner process-wide before route classification, keeping creation, status, cancellation and WebSocket ownership coherent without per-route fallback
      - a static renderer gate rejects direct product /api fetches outside the preselected transport; the two synthesis bypasses found by independent audit were migrated to api.get/api.post
      - independently re-audited Studio passes 547 Vitest files and 3877 tests, TypeScript, ESLint with zero errors, 133 Rust tests with one ignored probe, fmt, strict Clippy and Electron build
    review: >-
      Commit a9e57e2 freezes an executable R2 target without claiming a live
      WebSocket implementation. It distinguishes the sequenced native internal
      stream from the exact renderer-facing Studio-v1 envelope, preserves the
      legacy cancellation emission as job_failed with "Job was cancelled", and
      declares no event replay promise. Native selection cannot be enabled by
      this contract and never permits a Uvicorn/FastAPI retry after selection.
      Commits 1566bb8 and 4c3d8b9 implement the execution-agnostic bounded job
      registry and correct its metric/progress semantics against the Python
      oracle. Studio 7871230 adds the real bounded RFC 6455 connection manager
      and three frozen aliases on the unselected sidecar port. Studio c4b9446
      then wires the three status reads and five cancellation aliases to the
      same authoritative runtime, with a real HTTP/WebSocket differential and
      no FastAPI, Uvicorn, CPython or fallback. Integration commit 23d1596
      composes the job and run-detail capability assertions. The default
      ScientificJobExecutor remains deliberately unselected. Studio commit
      9814610 adds the two bounded durable execution-record reads from linked
      workspaces, joins known run identity through Store v5, and refuses
      symlinks, mutation and malformed snapshots without Python or FastAPI.
      Durable records remain WorkspaceStore-owned rather than in-memory
      registry state. Canonical commits a1e9707 and 161bf3f add the closed
      scientific-submission transport and live-preflighted Electron renderer
      selector. They make native transport reachable without making scientific
      execution available: scientific_execution remains false and the default
      executor refuses before mutation. Commit f50a0bd adds the future
      cpython-stdio-v1 acquisition boundary and verifies exact CPython identity,
      isolation, byte stability, stream/deadline limits and denial of listener
      ownership. It deliberately stays unselected because no current callable
      can consume the closed submission while leaving job/store/event ownership
      in Rust. Every acquisition failure is typed before mutation and no
      FastAPI/Uvicorn/legacy fallback is introduced. The general dynamic /ws protocol,
      selected scientific executor, remaining routes/scanners, installers,
      LOCK-STUDIO and R2-W2 remain open. A subsequent full route-inventory
      audit found a broader boundary outside this native request path: 284
      unmigrated HTTP operations plus dynamic /ws are still automatically
      preselected to scientific-plugin, whose Electron acquisition starts
      FastAPI/Uvicorn without an explicit user diagnostic choice. Native
      selection itself never falls back, but packaged product selection is not
      yet Rust-only. This P0 must fail closed before the Phase 2 claim.
      Commits 2ea5c52 through cdb9f46 close that P0 on the selected RC. A
      process-wide policy is resolved before any renderer request; packaged
      default is Rust-only, unmigrated HTTP/WS refuses before fetch or Python
      acquisition, and native selection has no retry. The visible CLI switch
      is the sole packaged diagnostic activation; the environment opt-in is
      accepted only in development. Diagnostic mode owns the entire renderer
      session rather than individual fallback routes. Independent audit found
      and corrected two direct synthesis fetches, added a whole-source bypass
      gate, synchronized the public contracts and operating docs, and approved
      cdb9f46 after all gates passed. Scientific execution remains false: the
      selected callable and Rust durable terminal bridge are separate open
      work, so LOCK-STUDIO and R2-W2 remain pending.
    rollback: remove only the additive job/WS target contract and test; retain the frozen Studio-v1 oracle and R1 sidecar behavior.
  - id: STU-006
    owner: F
    dependencies:
      - STU-002
      - STU-001
    state: r2_native_startup_and_packaged_rust_only_renderer_policy_qualified_cross_platform_archives_pending
    input_sha: cdb9f46de1643ab170934481cd9471e54ed3701c
    owned_files:
      - nirs4all-studio/electron/native-session-lifecycle.ts
      - nirs4all-studio/electron/scientific-plugin-lifecycle.ts
      - nirs4all-studio/electron/python-http-diagnostic-policy.ts
      - nirs4all-studio/electron/main.ts
      - nirs4all-studio/electron/preload.ts
      - nirs4all-studio/src/api/transport.ts
      - nirs4all-studio/src/lib/websocket.ts
      - nirs4all-studio/src/context/MlReadinessContext.tsx
      - nirs4all-studio/src/types/electron.d.ts
      - nirs4all-studio/electron/packaged-runtime-contract.ts
      - nirs4all-studio/scripts/native-runtime-contract.cjs
      - nirs4all-studio/scripts/build-native-sidecar.cjs
      - nirs4all-studio/scripts/build-archive-standalone.cjs
      - nirs4all-studio/scripts/smoke-archive-standalone.cjs
      - nirs4all-studio/.github/workflows/ci.yml
      - nirs4all-studio/.github/workflows/release-unified.yml
    acceptance_tests:
      - Electron session starts the Rust sidecar and creates the window without starting Uvicorn or requiring a Python environment
      - library/plugin-host acquisition is explicit and single-flight; it never owns the product HTTP port, scheduler, store, jobs or renderer transport
      - control-plane and scientific readiness, restart and interpreter-change lifecycles are separate; changing Python never restarts the sidecar
      - selected native requests never retry FastAPI; an unavailable or invalid CPython runtime disables the bounded library/plugin host without changing Rust backend ownership
      - selected Studio RC passes TypeScript, targeted ESLint and 47 focused Vitest tests after integration
      - packaged startup verifies a content-addressed Rust sidecar contract before opening the renderer; a missing or altered sidecar fails closed without starting Python
      - missing or altered bundled CPython disables the explicit library/plugin host while the Rust backend remains active during packaged startup smoke
      - the selected Linux package launches under Xvfb with the Rust sidecar as the sole product-port owner and no Python or Uvicorn process
      - full Studio qualification passes 3861 Vitest tests, TypeScript, ESLint with zero errors, 126 Rust tests with one ignored probe, rustfmt and Clippy with warnings denied
      - packaged default route selection is Rust-only and an available bundled CPython cannot start FastAPI/Uvicorn for an unmigrated route; typed refusal precedes fetch and process acquisition
      - packaged environment inheritance cannot activate Python HTTP; explicit CLI diagnostic selection remains whole-session and is reported through readiness/IPC
      - all renderer product fetch and WebSocket construction paths are guarded by source and behavior tests, with no native-to-Python retry
    review: >-
      Commit 2490396 removes eager BackendManager startup from Electron and
      preserves legacy backend:* IPC as lazy aliases while publishing explicit
      control:* and scientific:* roles. Commit 94688d8 synchronizes Studio
      lifecycle and packaging documentation. Canonical R2 packaging commit
      e6dc293 adds the content-addressed all-in-one runtime contract and makes
      the verified Rust sidecar mandatory before renderer startup. CPython is
      now bounded to the library/plugin-host role: absence or tamper disables
      that host while Rust remains active, with no backend fallback. A real
      Linux Electron package, archive smoke and Xvfb launch passed with the
      sidecar as sole product-port owner. Windows, macOS and heavy all-in-one
      CPython archives remain CI-only qualifications; the scientific executor,
      remaining plugin routes, LOCK-STUDIO and R2-W2 remain open. The package
      smoke covered startup and native readiness, not every renderer route.
      Route-inventory audit at f50a0bd proves that unmigrated requests still
      acquire the transitional FastAPI/Uvicorn process automatically when
      CPython is available. The runtime manifest therefore records the intended
      role but does not yet prove the product call graph; packaged default must
      refuse those paths unless an explicit diagnostic mode is selected.
      Selected Studio cdb9f46 now proves that call graph: Electron freezes the
      Rust-only default before startup, every renderer product request and
      connection is preselected, unmigrated paths refuse without acquiring
      Python, and packaged environment inheritance cannot enable FastAPI. Only
      the visible whole-session diagnostic switch can do so. A static source
      gate prevents future direct /api fetch bypasses. The selected increment
      passes the full 3877-test Vitest suite, TypeScript, ESLint, Rust fmt,
      strict Clippy, 133 Rust tests with one ignored probe and Electron build.
      The previous f50a0bd sidecar-readiness artifact is historical and must be
      regenerated for cdb9f46 before GATE-001 can use the new selection.
    rollback: revert only the lazy lifecycle/IPC/UI readiness adapter; preserve sidecar state and user Python environments.
  - id: W1-001
    owner: G
    dependencies:
      - SAVE-001
    state: complete
    input_sha: 3097da5658d0abca575d36db1f6a82f1f5d55490
    owned_files:
      - dag-ml/docs/contracts/training_contract_conformance_pack.v1.json
      - dag-ml/docs/contracts/training_replay_contract_conformance_pack.v1.json
      - dag-ml/parity/training/generate_training_replay_fixtures.py
      - dag-ml/parity/training/tests/test_training_replay_contracts.py
      - dag-ml/scripts/validate_training_replay_contracts.py
    acceptance_tests:
      - Base/replay/conformal generators are byte deterministic and both validators passed
      - Base/replay/conformal packs reconstruct exactly with 92/140/53 leaves and replay pins derived from the base output
      - single-byte tamper of Cargo.toml, runtime/mod.rs, conformal.rs, conformal_runtime.rs and covered HPO documentation is rejected by both validators
    review: >-
      Two independent Sol-xhigh reviews ACCEPT the refreshed HPO/Conformal closure:
      base=92 (705a6a0224dde31805d8acba39864992ed1b58c2ed8e67806a5c6b67c8d4a9ec,
      9fc9d2f10abe57132ff44fc97fb755e61bf4c9ee3cf4ee4ead822bcb786a3874), replay=140
      (7354f69c53b8abd578e493191fe49ec8460c372d2755d71f2040f7cd49f54dcf,
      2038edaea7b576493cd46ef965679841ef8889d78e57b6da1d64cb8dca8ac1ab), conformal=53
      unchanged. Two canonical passes produce closure SHA ef7d73e3b577183e773314cfd0b12cac301abc6a4749b96a0d3b55389251c938;
      both validators and 221 parity tests pass. Packs remain local/uncommitted and still require the selected dag-ml-data CI topology before a release claim.
    rollback: reverse only generator-derived pack/pin/test changes as one unit; never patch leaf hashes manually.
  - id: TOOLS-001
    owner: D
    dependencies:
      - SAVE-001
    state: complete
    input_sha: 3ad7e83da57e517a035d56407bd3bab824083230
    owned_files:
      - nirs4all-tools/src/nirs4all_tools/cli.py
      - nirs4all-tools/src/nirs4all_tools/commands.py
      - nirs4all-tools/src/nirs4all_tools/policy.py
      - nirs4all-tools/README.md
      - nirs4all-tools/CONTRIBUTING.md
      - nirs4all-tools/tests/
    acceptance_tests:
      - semantic workspace-v2 conversion refuses exit 20 before output/checkpoint
      - copy-only snapshot/resume owns the complete output inventory; custom contract names and profile changes are covered
      - full suite 88 passed with only explicit TOOL-SEM-001 skip; ruff and mypy passed
    review: Sol-xhigh ACCEPT. This item proves preservation copy-only or fail-closed refusal, not a semantic archive V1 writer or convert-load-predict migration.
    rollback: reverse only the Tools-owned files; never delete converted user output automatically.
  - id: PAR-002
    owner: G
    dependencies:
      - CAP-001
      - DATA-001
      - MTH-001
    state: selected_bounded_native_first_dual_oracle_methods_runtime_parity_pending
    input_sha: 71dce9d9ecfc9e872393b96f19cae8aded34df41
    owned_files:
      - nirs4all/nirs4all/api/run.py
      - nirs4all/nirs4all/api/result.py
      - nirs4all/nirs4all/pipeline/engine.py
      - nirs4all/nirs4all/compatibility_ledger.json
      - nirs4all/nirs4all/pipeline/keyword_registry.py
      - nirs4all/docs/source/
      - nirs4all/tests/unit/api/test_dual_run_oracle.py
    acceptance_tests:
      - installed-wheel ledger resource is byte-synchronized with docs and resolved outside checkout
      - real native PLS/KFold dual probe passed with exact OOF coverage, winner/split/y_pred and ledger bands
      - unsupported capabilities, calibration/tuning, non-exact types/data and native result environment refuse before legacy fallback
      - native executes and exposes complete finite evidence before the temporary legacy workspace is allocated; cleanup holds on success, refusal and mismatch
      - run.engine registry and user documentation enumerate legacy, dag-ml, native and dual consistently
    review: >-
      Sol-xhigh ACCEPT. Scope is explicitly partial: NumPy regression tuple,
      exact KFold(shuffle=False), exact PLSRegression and strict no-artifact
      options only. Python RC 71dce9d9 now contains the minimal reconciled
      implementation plus finite-tolerance and native-registry corrections.
      The native DAG-ML leg is executed and validated first; unsupported or
      incomplete native evidence never starts legacy. The explicit legacy
      oracle lives only in a removed TemporaryDirectory and the native result
      is returned after an exact/ledger-bounded semantic comparison. The
      independently built HEAD wheel SHA-256 is
      cda2bf974c087562d3a7e5872a7a61912721f35ebd187ee1632a982a1ec58746.
      This remains orchestration parity over Python sklearn PLS, not proof of
      Methods/libn4m numerical execution; that broader integration stays open.
    rollback: remove only the dual-run implementation/resource/docs/tests; preserve engine=legacy and native single-run behavior.
  - id: PROV-001
    owner: C
    dependencies:
      - DATA-001
    state: complete
    input_sha: 91302fdbc38474e94750184469923f9b2bbb0e9d
    owned_files:
      - dag-ml-data/crates/dag-ml-data-core/src/coordinator.rs
      - dag-ml-data/crates/dag-ml-data-core/src/handle.rs
      - dag-ml-data/crates/dag-ml-data-provider/src/json.rs
      - dag-ml-data/crates/dag-ml-data-capi/src/lib.rs
      - dag-ml-data/docs/
    acceptance_tests:
      - real JsonInMemoryProvider by_filter route preserves supplied identity/relations/weights/partition-source provenance
      - unknown/null/empty/mixed selector predicates refuse pre-projection as typed validation, including C ABI
      - workspace fmt/clippy/tests, contracts and schema fingerprint passed (274 tests, 2 ignored perf probes)
    review: Sol-xhigh ACCEPT. Fold declarations remain envelope provenance only; Arrow C ABI is documented as IDs/is_augmented only, while full relation metadata/tags remain core/JSON.
    rollback: reverse only the nine dag-ml-data provider/docs files; do not alter IO bridge or global release lock.
  - id: GOV-001
    owner: release-captain
    dependencies:
      - ARCH-001
    state: pending
    input_sha: a34126d43c68e5122ba35a5ac8e2b36e4da56b61
    owned_files:
      - docs/contracts/release/migration-work-ledger.yaml
    acceptance_tests: []
    review: pending
    rollback: ownership records are additive and reversible
next_batch:
  gate: r2-api-studio-web-and-lock-closure
  condition: >-
    Finish general API prediction/run coverage beyond the bounded multi-target
    Methods PLS lane, Studio scientific submission and renderer selection, and
    the remaining data binding matrix; connect all four runtime-specific
    launchers to the strict GATE-001 harness, run differential/performance and
    install gates, rebuild Web WASM from those exact artifacts, and regenerate
    a reconstructible selected-workspace lock before CUT-001.
