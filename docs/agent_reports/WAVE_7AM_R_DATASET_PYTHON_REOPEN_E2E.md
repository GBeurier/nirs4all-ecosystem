# Wave 7AM - R Dataset Python Reopen E2E

Date: 2026-07-07

## Scope

Hardened `e2e-r-dataset-io-pipeline-save` from declarative Python reopen/rerun coverage to executable evidence.

## Files Modified

- `docs/contracts/e2e/cross-language-scenarios.n4a.json`
- `tests/test_e2e_scenarios.py`
- `nirs4all-core` submodule pointer to `1ddc400`

Upstream core change:

- `nirs4all-core/scripts/e2e/reopen_r_dataset_io_pipeline.py`

## Decisions

- Promoted `python_open_pipeline` and `python_rerun_pipeline` for the R dataset IO pipeline scenario from `contract` to `strict`.
- Added a Python ledger artifact, `python-reopen-ledger.json`, generated by reopening the R-produced workspace/pipeline and rerunning the pipeline through the Python aggregate.
- Kept `papers_export`, `repository_forced_best_refit`, and `wasm_web_reuse` as gaps for this scenario.
- Did not modify `nirs4all-ui`; that repo is reserved for the concurrent `nirs4all-quality` agent.

## Validation

- `python3 /home/delete/nirs4all/nirs4all-core/scripts/e2e/reopen_r_dataset_io_pipeline.py --in /home/delete/nirs4all/nirs4all-ecosystem/.n4a-e2e-artifacts/r-dataset-io-pipeline --out /home/delete/nirs4all/nirs4all-ecosystem/.n4a-e2e-artifacts/r-dataset-io-pipeline/python-reopen-ledger.json --workspace-root /home/delete/nirs4all`
- `python3 scripts/n4a_e2e_scenarios.py validate`
- `python3 scripts/n4a_e2e_scenarios.py plan --json`
- `python3 scripts/n4a_e2e_scenarios.py coverage --json`
- `python3 scripts/n4a_e2e_scenarios.py coverage`
- `python3 scripts/n4a_e2e_scenarios.py evidence --scenario e2e-r-dataset-io-pipeline-save --max-age-seconds 99999999`
- `python3 -m pytest -q tests/test_e2e_scenarios.py`
- `git diff --check`

Results:

- E2E manifest validation: 11 scenarios ready, 0 blocked.
- Evidence validation: `e2e-r-dataset-io-pipeline-save` verified, 10 artifacts, 0 failures.
- Pytest: 101 passed.
- Debt after promotion: `v1_contract_phase_count=11`, `v1_gap_phase_count=31`.

## Risks

- The ledger uses existing R scenario artifacts under `.n4a-e2e-artifacts`; fresh artifact generation should be rerun before the final parity batch.
- This is a targeted hardening, not full parity. Full Python-reference parity remains deferred until after larger batches.
